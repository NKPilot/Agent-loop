---
phase: 01-agent-core-loop
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/loopai/consumers/__init__.py
  - src/loopai/consumers/jsonl_logger.py
  - src/loopai/consumers/cli_renderer.py
  - tests/test_jsonl_logger.py
  - tests/test_cli_renderer.py
autonomous: true
requirements:
  - CORE-07
  - CORE-03

must_haves:
  truths:
    - "JSONL Logger creates a per-session file named YYYY-MM-DD_{session_id}.jsonl in logs/sessions/ on session start"
    - "JSONL Logger writes each event from the EventBus as one JSON line with seq, ts, session_id, and event fields"
    - "JSONL Logger flushes after each write and fsyncs on stop for crash resilience"
    - "CLI Renderer displays step panels, live token streaming, and tool call cards in the terminal using Rich Live"
    - "Both consumers subscribe to the EventBus and stop cleanly on None sentinel"
  artifacts:
    - path: "src/loopai/consumers/jsonl_logger.py"
      provides: "JSONLLogger 消费者，将 EventBus 事件写入磁盘"
      min_lines: 60
      exports: ["JSONLLogger"]
    - path: "src/loopai/consumers/cli_renderer.py"
      provides: "CLIAgentRenderer 消费者，通过 Rich 在终端渲染 agent 活动"
      min_lines: 80
      exports: ["CLIAgentRenderer"]
  key_links:
    - from: "src/loopai/consumers/jsonl_logger.py"
      to: "src/loopai/events/bus.py"
      via: "start() 中的 bus.subscribe('*')"
      pattern: "bus\\.subscribe"
    - from: "src/loopai/consumers/cli_renderer.py"
      to: "src/loopai/events/bus.py"
      via: "run() 中的 bus.subscribe('*')"
      pattern: "bus\\.subscribe"
    - from: "src/loopai/consumers/jsonl_logger.py"
      to: "logs/sessions/ 目录"
      via: "open() 使用追加模式"
      pattern: "open.*jsonl.*'a'"
---

<objective>
构建两个 Event Bus 消费者，使 agent 活动可观测。JSONL Logger 将每个事件持久化到磁盘，用于审计和回放 (CORE-07, D-10, D-11)。CLI Renderer 使用 Rich 在终端显示实时 agent 思考过程 (CORE-03, D-03)。两者都是独立的 asyncio 任务，使用 "*" 通配符主题订阅 EventBus。

目的: 在 FSM 开始产生事件之前创建输出层 -- 磁盘持久化和终端显示。
产出: 带 7 个通过测试的 JSONLLogger，带 5 个通过测试的 CLIAgentRenderer。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-agent-core-loop/01-CONTEXT.md
@.planning/phases/01-agent-core-loop/01-RESEARCH.md

<interfaces>
来自 Plan 01 -- EventBus: publish(), subscribe("*") -> asyncio.Queue, shutdown 哨兵 (None)
来自 Plan 01 -- 事件模式: 全部 13 种事件类型，以带 event_type 鉴别器的字典形式

已锁定决策: D-10 (1:1 事件到行映射), D-11 (文件命名: YYYY-MM-DD_{session_id}.jsonl), D-03 (双粒度流式显示)

RESEARCH.md 模式 6 (JSONL): 仅追加、每次写入 flush、停止时 fsync、pydantic 序列化
RESEARCH.md 陷阱 5: 原子性全量重建显示更新，防止步骤显示交错
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1: 实现 JSONL Logger 消费者 (CORE-07, D-10, D-11)</name>
  <files>src/loopai/consumers/__init__.py, src/loopai/consumers/jsonl_logger.py, tests/test_jsonl_logger.py</files>
  <read_first>src/loopai/events/bus.py (subscribe/unsubscribe 签名), .planning/phases/01-agent-core-loop/01-RESEARCH.md (模式 6: JSONL 事件日志器)</read_first>
  <action>
    创建 src/loopai/consumers/__init__.py (空文件)。实现 src/loopai/consumers/jsonl_logger.py:
    JSONLLogger 类，__init__(session_id, log_dir="logs/sessions") 创建 Path(log_dir)，mkdir parents，以追加模式 utf-8 打开文件，立即执行 os.chmod 0o600。
    start(bus) 方法: 订阅 "*" 主题，存储队列，返回 asyncio.create_task(self._consume())。
    _consume() 协程: 在 queue.get() 上循环，遇到 None 哨兵时 break，为每个事件调用 _write()。
    _write(event) 方法: 构建包含 seq、ts (ISO 8601 UTC)、session_id、**event 的条目；写入 json.dumps(entry, ensure_ascii=False) + "\n"；每次写入后 flush。
    stop() 方法: os.fsync，关闭文件。

    创建 tests/test_jsonl_logger.py，包含 7 个使用 tmp_path fixture 的测试: test_log_file_created_and_permissions (验证 0o600)、test_event_to_line_mapping (3 个事件 -> 3 行)、test_entry_fields (验证 seq/ts/session_id/event_type 键)、test_append_not_overwrite (重新打开为追加)、test_flush_after_write (事件在 stop 前已写入磁盘)、test_shutdown_sentinel (None -> 干净退出)、test_session_id_in_filename (正则匹配 YYYY-MM-DD_*.jsonl)。使用真实的 EventBus 实例。运行 `python -m pytest tests/test_jsonl_logger.py -v`。
  </action>
  <verify>
    <automated>python -m pytest tests/test_jsonl_logger.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 日志文件位于 logs/sessions/YYYY-MM-DD_{session_id}.jsonl，按 D-11
    - 文件权限 0o600 (仅所有者读写)
    - 1:1 事件到行映射，按 D-10
    - 每行包含 seq、ts、session_id 和所有事件字段
    - 追加模式 (使用相同 session_id 重新打开为追加，不覆盖)
    - 每次写入后 flush；stop 时 fsync
    - 消费者在 None 哨兵时干净退出
    - 7 个测试通过
  </acceptance_criteria>
  <done>JSONL Logger 创建结构化日志文件，按会话隔离，1:1 事件到行映射，0o600 权限。</done>
</task>

<task type="auto">
  <name>任务 2: 实现 CLI Renderer 消费者，使用 Rich Live 显示 (CORE-03)</name>
  <files>src/loopai/consumers/cli_renderer.py, tests/test_cli_renderer.py</files>
  <read_first>src/loopai/events/bus.py (subscribe 签名), src/loopai/events/schemas.py (事件字段名称)</read_first>
  <action>
    实现 src/loopai/consumers/cli_renderer.py:
    CLIAgentRenderer 类，__init__(bus) 存储 bus 引用并初始化状态 (current_step=0, max_steps=15, step_content="", tool_calls 列表, current_state="REASON", exit_reason="")。
    build_renderable() 方法: 返回 Rich Layout，包含顶部面板 (步骤进度)、中间面板 (step_content 的 Markdown)、底部面板 (工具调用状态卡片，如果有的话)。
    run(max_steps) 方法: 订阅 "*"，进入 Rich Live 上下文管理器 (refresh_per_second=10, transient=True, Console())，在 queue.get() 上循环，调用 _handle_event() 然后原子地 live.update(build_renderable())。遇到 None 哨兵时 break。
    _handle_event(event) 方法: 按 event_type 路由 -- step_start 重置 content/tool_calls/设置 step_num，llm_token 累积 content_delta，tool_call_start/done/result 跟踪生命周期，session_end 更新最终状态，budget_warning/error 追加指示文本。
    关键: 按 RESEARCH.md 陷阱 5 -- 始终从当前状态重建完整 Layout 树，永不增量更新面板。在 Live 上使用 transient=True。

    创建 tests/test_cli_renderer.py，包含 5 个直接操作 _handle_event 的测试 (单元测试状态机，跳过 Live 上下文管理器): test_step_content_accumulation、test_step_reset_on_step_start、test_tool_call_lifecycle、test_build_renderable_no_crash、test_session_end_updates_state。运行 `python -m pytest tests/test_cli_renderer.py -v`。
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_renderer.py -v</automated>
  </verify>
  <acceptance_criteria>
    - CLIAgentRenderer 将 LLMToken content_delta 累积到 step_content
    - StepStart 重置 step_content、tool_calls，并设置 current_step
    - 工具调用生命周期 (start/args/done/result) 正确跟踪
    - build_renderable() 无崩溃返回 Rich Layout
    - SessionEnd 正确更新最终状态和 exit_reason
    - Rich Live 使用 transient=True 和原子性全量重建更新
    - 5 个测试通过
  </acceptance_criteria>
  <done>CLI Renderer 提供 agent 思考轨迹的 Rich Live 终端显示，使用原子性全量重建更新。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| EventBus -> JSONL Logger | 事件写入磁盘。文件权限限制访问。 |
| JSONL Logger -> 文件系统 | 会话日志位于 logs/sessions/。包含 agent 活动，包括工具输出。 |

## STRIDE 威胁登记表

| 威胁 ID | 类别 | 组件 | 处置方式 | 缓解计划 |
|---------|------|------|---------|---------|
| T-04-01 | 信息泄露 | JSONLLogger | 缓解 | 打开文件后立即设置权限 0o600；logs/sessions/ 目录权限 0o700 |
| T-04-02 | 篡改 | JSONLLogger | 接受 | 仅追加模式防止会话内修改；Phase 1 中无日志完整性验证 |
| T-04-03 | 拒绝服务 | CLIAgentRenderer | 接受 | Rich Live 显示为尽力而为；有界队列可能对慢速终端丢弃事件 -- 对 CLI 可接受 |
| T-04-04 | 信息泄露 | JSONLLogger._write() | 缓解 | 绝不将 api_key 写入日志；AgentConfig 使用 SecretStr；LLMClient 绝不将其包含在事件中 |
</threat_model>

<verification>
运行 `python -m pytest tests/test_jsonl_logger.py tests/test_cli_renderer.py -v` -- 12 个测试必须通过。
运行 `python -c "from loopai.consumers.jsonl_logger import JSONLLogger; from loopai.consumers.cli_renderer import CLIAgentRenderer; print('Consumer imports OK')"` -- 成功。
</verification>

<success_criteria>
两个消费者均可运行。JSONL Logger 以适当权限写入 1:1 日志。CLI Renderer 显示实时 agent 活动。FSM 可以将它们作为后台任务接入。
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-agent-core-loop/01-04-SUMMARY.md`
</output>
