---
phase: 08-dynamic-tool-core
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - src/loopai/main.py
  - src/loopai/api/routes/control.py
  - src/loopai/tools/prompt_builder.py
autonomous: true
requirements:
  - DYN-01
  - DYN-08
  - DYN-24

must_haves:
  truths:
    - "generate_tool 内置工具在 create_agent_components 中注册到 ToolRegistry，Agent 可在会话中调用"
    - "POST /api/sessions/{session_id}/confirm-tool-creation 端点存在，用户确认/拒绝后解除 Agent 阻塞"
    - "build_system_prompt 追加已注册动态工具列表，Agent 知道可调用哪些动态工具"
    - "会话关闭时，会话级动态工具从 ToolRegistry 自动移除"
  artifacts:
    - path: "src/loopai/main.py"
      provides: "create_agent_components 中注册 generate_tool，传递 dynamic_creator 到 active_sessions"
      contains: "dynamic_creator"
    - path: "src/loopai/api/routes/control.py"
      provides: "confirm-tool-creation REST 端点"
      contains: "confirm_tool_creation"
    - path: "src/loopai/tools/prompt_builder.py"
      provides: "动态工具列表注入 system prompt"
      contains: "dynamic"
  key_links:
    - from: "src/loopai/main.py create_agent_components"
      to: "src/loopai/tools/registry.py register_meta"
      via: "generate_tool 注册"
      pattern: "registry\\.register.*generate"
    - from: "src/loopai/api/routes/control.py confirm_tool_creation"
      to: "DynamicToolCreator.respond()"
      via: "active_sessions[session_id]['dynamic_creator']"
      pattern: "dynamic_creator\\.respond"
---

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dynamic-tool-core/08-CONTEXT.md
@.planning/phases/08-dynamic-tool-core/08-RESEARCH.md

<interfaces>
从 src/loopai/main.py (现有 create_agent_components 函数):
```python
def create_agent_components(config, prompt, bus, max_steps_override=None) -> dict:
    registry = ToolRegistry()
    executor = ToolExecutor(registry)
    permission_guard = PermissionGuard(bus, confirmation_timeout=config.confirmation_timeout)
    bash_fn = create_bash_tool(working_dir=config.tool_working_dir)
    registry.register(bash_fn)
    register_disk_tools(registry, working_dir=config.tool_working_dir)
    system_prompt = build_system_prompt(registry, working_dir=config.tool_working_dir)
    # ... AgentTool 注册 ...
    # ... Session/FSM 创建 ...
    return {
        "session": session, "fsm": fsm, "logger": logger,
        "permission_guard": permission_guard, "registry": registry,
        "executor": executor, "checkpoint_manager": checkpoint_manager,
        "failure_registry": failure_registry, "bus": bus,
    }
```

从 src/loopai/api/routes/control.py (现有 confirm_session 端点):
```python
@router.post("/sessions/{session_id}/confirm")
async def confirm_session(session_id: str, body: ConfirmRequest, request: Request) -> dict:
    active_sessions = request.app.state.active_sessions
    # active_sessions[session_id] = {"session", "fsm", "task", "logger_task", "permission_guard", "status"}
    entry = active_sessions[session_id]
    permission_guard = entry.get("permission_guard")
    permission_guard.respond(body.confirmation_id, body.approved)
    return {"confirmation_id": ..., "approved": ..., "responded": True}

@router.post("/sessions/{session_id}/start")
async def start_session(body, request):
    # ...
    app_state.active_sessions[session.session_id] = {
        "session": session, "fsm": fsm, "task": agent_task,
        "logger_task": logger_task, "permission_guard": permission_guard,
        "status": "running",
    }
```

从 src/loopai/tools/prompt_builder.py (现有 build_system_prompt):
```python
def build_system_prompt(registry: ToolRegistry, working_dir: str = ".") -> str:
    lines = ["你是一个具有系统诊断能力的 AI 助手。", "", "## 可用工具"]
    # ... 遍历 registry.get_schemas() 构建工具列表 ...
    return "\n".join(lines)
```

从 src/loopai/tools/dynamic_creator.py (08-03 创建):
```python
class DynamicToolCreator:
    def __init__(self, registry, bus, session_id, sandbox, persistence): ...
    async def generate_tool(self, name, description, code, test_code, language, param_schema) -> ToolResult: ...
    def respond(self, confirmation_id: str, approved: bool, config: dict) -> None: ...
    def _pending_confirmations: dict[str, tuple[asyncio.Event, dict | None]]

def create_generate_tool_fn(creator: DynamicToolCreator) -> Callable: ...
```

从 src/loopai/api/schemas.py (08-01 新增):
```python
class ConfirmToolCreationRequest(BaseModel):
    confirmation_id: str
    approved: bool
    persistence: Literal["session", "sandbox", "project"] = "session"
    extra_dirs: list[str] = []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 create_agent_components 中注册 generate_tool 并传递 dynamic_creator</name>
  <files>src/loopai/main.py</files>
  <read_first>src/loopai/main.py, src/loopai/tools/dynamic_creator.py</read_first>
  <action>
修改 `src/loopai/main.py` 的 `create_agent_components` 函数：

1. **导入新增模块**（在现有 import 块末尾追加）：
   ```python
   from loopai.tools.dynamic_creator import DynamicToolCreator, create_generate_tool_fn
   from loopai.tools.sandbox import SandboxExecutor
   from loopai.tools.tool_persistence import ToolPersistenceManager
   ```

2. **在工具注册段（bash_tool/disk_tools 注册之后，prompt_builder 调用之前）创建动态工具基础设施并注册 generate_tool**：
   ```python
   # ── 动态工具创建基础设施（v1.1 Phase 8）─────────────────────────
   sandbox = SandboxExecutor()
   tool_persistence = ToolPersistenceManager()
   dynamic_creator = DynamicToolCreator(
       registry=registry,
       bus=bus,
       session_id=session.session_id,  # Session 已创建，需在此之前确保 session 已存在
       sandbox=sandbox,
       persistence=tool_persistence,
   )
   generate_tool_fn = create_generate_tool_fn(dynamic_creator)
   registry.register(generate_tool_fn)
   ```

   注意：Session 在 `create_agent_components` 中创建于工具注册块之前（查看现有代码第 117 行：`session = Session(config=config)`），但 session.session_id 在注册块之后才确定。实际执行时需调整：先生成 session，再用 session_id 创建 DynamicToolCreator。具体位置在 `session = Session(config=config)` 之后，`build_system_prompt` 调用之前。

3. **在 system_prompt 构建后追加动态工具指引**：在 system_prompt 字符串末尾追加段落（在子 Agent 说明之后）：
   ```python
   system_prompt += (
       "\n\n## 动态工具创建\n"
       "你可以通过调用 `generate_tool` 工具来创建新的动态工具。提供工具名称、描述、"
       "Python/Bash 代码、测试用例代码、编程语言（python/bash）和参数 JSON Schema。"
       "系统会自动进行语法检查和安全扫描，然后弹出确认窗口供用户审批。"
       "审批通过并自测成功后，新工具将以 `dynamic.{hash}_{name}` 格式注册到系统中，"
       "你可以像其他工具一样调用它。\n"
       "使用建议：当现有的静态工具无法满足需求时，考虑创建动态工具来扩展能力。"
   )
   ```

4. **在返回字典中添加 dynamic_creator**：
   ```python
   return {
       # ... 现有字段 ...
       "dynamic_creator": dynamic_creator,
   }
   ```

5. **更新 start_session 端点**（在 control.py 中——本任务仅在 main.py 侧确保 dynamic_creator 可传递）：在 active_sessions 注册时添加：
   在 `control.py` 的 `start_session` 函数中，修改 `app_state.active_sessions[session.session_id]` 字典，添加：
   ```python
   "dynamic_creator": components.get("dynamic_creator"),
   ```
</action>
<verify>
<automated>python -c "
# 验证 create_agent_components 返回 dynamic_creator
import asyncio
from loopai.config import load_config
from loopai.events.bus import EventBus
from loopai.main import create_agent_components

config = load_config(None)
bus = EventBus()
components = create_agent_components(config, 'test prompt', bus)
assert 'dynamic_creator' in components, 'dynamic_creator missing from components'
assert components['dynamic_creator'] is not None
# 验证 generate_tool 已注册
registry = components['registry']
meta = registry.get('generate_tool')
assert meta is not None, 'generate_tool not registered'
assert meta.name == 'generate_tool'
print('create_agent_components OK')
"</automated>
</verify>
<acceptance_criteria>
- create_agent_components 创建 DynamicToolCreator+SandroxExecutor+ToolPersistenceManager 实例
- generate_tool 通过 registry.register() 注册到 ToolRegistry（作为 @tool 装饰的静态工具）
- system_prompt 追加 "## 动态工具创建" 段落
- 返回字典包含 "dynamic_creator" 键
- start_session 端点将 dynamic_creator 存入 active_sessions
</acceptance_criteria>
<done>generate_tool 集成到 Agent 组件工厂——Agent 可在会话中调用 generate_tool 创建动态工具</done>
</task>

<task type="auto">
  <name>Task 2: 添加 confirm-tool-creation REST 端点 + 会话清理</name>
  <files>src/loopai/api/routes/control.py</files>
  <read_first>src/loopai/api/routes/control.py, src/loopai/api/schemas.py, src/loopai/tools/dynamic_creator.py</read_first>
  <action>
修改 `src/loopai/api/routes/control.py`：

1. **导入 ConfirmToolCreationRequest**：在现有 imports 中添加：
   ```python
   from loopai.api.schemas import (
       ConfirmRequest,
       ConfirmToolCreationRequest,  # 新增
       SendMessageRequest,
       SendMessageResponse,
       StartSessionRequest,
       StartSessionResponse,
   )
   ```

2. **添加 /confirm-tool-creation 端点**（在 confirm_session 端点之后）：
   ```python
   @router.post("/sessions/{session_id}/confirm-tool-creation")
   async def confirm_tool_creation(
       session_id: str,
       body: ConfirmToolCreationRequest,
       request: Request,
   ) -> dict:
       """响应待处理的工具创建确认请求。
   
       在 app.state.active_sessions 中查找会话，获取 DynamicToolCreator，
       并调用 respond() 传入用户的确认/拒绝决定和配置。
       DynamicToolCreator.respond() 设置 asyncio.Event 解除 generate_tool 管道的阻塞。
   
       Args:
           session_id: 目标会话 ID。
           body: 包含 confirmation_id、approved、persistence、extra_dirs 的请求体。
           request: FastAPI 请求对象。
   
       Returns:
           包含 confirmation_id、approved、responded 键的字典。
   
       Raises:
           HTTPException 404: 会话未找到、dynamic_creator 不存在或 confirmation_id 无效。
       """
       active_sessions = request.app.state.active_sessions
   
       if session_id not in active_sessions:
           raise HTTPException(
               status_code=404, detail=f"Session '{session_id}' not found"
           )
   
       entry = active_sessions[session_id]
       dynamic_creator = entry.get("dynamic_creator")
   
       if dynamic_creator is None:
           raise HTTPException(
               status_code=404,
               detail=f"No dynamic tool creator for session '{session_id}'",
           )
   
       # 验证 confirmation_id 是否为待处理状态
       if body.confirmation_id not in dynamic_creator._pending_confirmations:
           raise HTTPException(
               status_code=404,
               detail=f"Confirmation '{body.confirmation_id}' not found or already responded",
           )
   
       # 构造配置 dict 传给 DynamicToolCreator
       config = {
           "approved": body.approved,
           "persistence_level": body.persistence,
           "extra_dirs": body.extra_dirs,
       }
       dynamic_creator.respond(body.confirmation_id, body.approved, config)
   
       return {
           "confirmation_id": body.confirmation_id,
           "approved": body.approved,
           "responded": True,
       }
   ```

3. **在 _run_and_cleanup 中添加会话级动态工具清理逻辑**：
   在 `_run_and_cleanup` 的 `finally` 块中，在设置 `entry["status"] = "completed"` 之前，添加：
   ```python
   # 清理会话级动态工具（DYN-24）
   dynamic_creator = entry.get("dynamic_creator")
   if dynamic_creator is not None:
       registry = dynamic_creator._registry
       for meta in registry.list_dynamic():
           # 会话级工具：persistence tag 不含 "persist:"
           if not any(tag.startswith("persist:") for tag in meta.tags):
               try:
                   registry.remove(meta.name)
               except ValueError:
                   pass  # 跳过静态工具删除保护
   ```

4. **在 start_session 端点中**：修改 `app_state.active_sessions[session.session_id]` 字典，添加 `"dynamic_creator": components.get("dynamic_creator")`。
</action>
<verify>
<automated>python -c "
from loopai.api.routes.control import router
# 验证 /confirm-tool-creation 路由已注册
routes = [(r.path, list(r.methods)) for r in router.routes]
confirm_routes = [r for r in routes if 'confirm-tool-creation' in r[0]]
assert len(confirm_routes) > 0, 'confirm-tool-creation route not found'
assert 'POST' in confirm_routes[0][1], 'confirm-tool-creation should be POST'
print('confirm-tool-creation route OK')

# 验证 import 不报错
from loopai.api.schemas import ConfirmToolCreationRequest
req = ConfirmToolCreationRequest(confirmation_id='abc123', approved=True, persistence='sandbox', extra_dirs=['/tmp'])
assert req.persistence == 'sandbox'
assert req.extra_dirs == ['/tmp']
print('ConfirmToolCreationRequest validation OK')
"</automated>
</verify>
<acceptance_criteria>
- POST /api/sessions/{session_id}/confirm-tool-creation 端点存在
- 端点验证 session_id 存在于 active_sessions
- 端点验证 dynamic_creator 存在且 confirmation_id 有效
- 调用 dynamic_creator.respond() 传递 approved + persistence + extra_dirs
- 无效 confirmation_id 返回 404
- 会话关闭时，会话级动态工具（无 persist: tag）从 Registry 移除
- start_session 将 dynamic_creator 存入 active_sessions
</acceptance_criteria>
<done>confirm-tool-creation REST 端点可用——前端确认/拒绝后解除 Agent 管道阻塞；会话清理逻辑就位</done>
</task>

<task type="auto">
  <name>Task 3: 更新 build_system_prompt 追加动态工具列表</name>
  <files>src/loopai/tools/prompt_builder.py</files>
  <read_first>src/loopai/tools/prompt_builder.py</read_first>
  <action>
修改 `src/loopai/tools/prompt_builder.py` 的 `build_system_prompt` 函数：

在现有工具列表构建后（安全/中等/危险工具遍历之后），添加动态工具列表段落：

```python
# 动态工具段落（v1.1 Phase 8）
dynamic_tools = [
    m for m in registry.list_all() if m.is_dynamic
]
if dynamic_tools:
    lines.append("")
    lines.append("## 动态工具")
    lines.append("以下是由 Agent 创建并通过用户审批的动态工具：")
    for meta in dynamic_tools:
        pers_tag = next((t for t in meta.tags if t.startswith("persist:")), "persist:session")
        persistence_label = pers_tag.replace("persist:", "")
        lines.append(
            f"- **{meta.name}** — {meta.description} "
            f"（动态工具，{persistence_label}级持久化）"
        )

return "\n".join(lines)
```

此逻辑在现有 `return "\n".join(lines)` 之前插入。动态工具列表为空时不追加段落。
</action>
<verify>
<automated>python -c "
from loopai.tools.prompt_builder import build_system_prompt
from loopai.tools.registry import ToolRegistry
from loopai.tools.types import ToolMetadata

# 测试空动态工具列表
registry = ToolRegistry()
prompt = build_system_prompt(registry)
assert '动态工具' not in prompt  # 无动态工具时不显示

# 测试有动态工具时
from loopai.tools.types import PermissionLevel
meta = ToolMetadata(
    name='dynamic.a1b2c3d4_test',
    description='测试动态工具',
    permission_level=PermissionLevel.MODERATE,
    is_dynamic=True,
    tags=['dynamic', 'lang:python', 'persist:sandbox'],
    func_ref=lambda: None,
)
registry.register_meta(meta, is_dynamic=True)
prompt = build_system_prompt(registry)
assert '动态工具' in prompt
assert 'dynamic.a1b2c3d4_test' in prompt
assert '测试动态工具' in prompt
assert 'sandbox级持久化' in prompt or 'sandbox' in prompt
print('build_system_prompt dynamic tools OK')
"</automated>
</verify>
<acceptance_criteria>
- build_system_prompt 遍历 registry.list_all() 筛选 is_dynamic=True 的工具
- 有动态工具时追加 "## 动态工具" 段落，列出名称+描述+持久化级别
- 无动态工具时不追加任何动态工具相关内容
- 持久化级别从 tags 中提取（persist:session/sandbox/project）
</acceptance_criteria>
<done>system prompt 包含已注册动态工具列表——Agent 知晓可调用的动态工具</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → POST /confirm-tool-creation | 用户确认数据跨信任边界；需验证 session 归属 + confirmation_id 有效性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Elevation of Privilege | control.py confirm_tool_creation | mitigate | 验证 session_id 在 active_sessions 中；验证 dynamic_creator 存在；验证 confirmation_id 有效且未过期；无效请求返回 404 |
| T-08-13 | Elevation of Privilege | prompt_builder.py 动态工具描述注入 | mitigate | 工具描述截断 max 500 字符（在 DynamicToolCreator 构造时限制）；使用固定模板包装（"以下是由 Agent 创建..."）而非直接裸注 Agent 提供的描述 |
| T-08-14 | Denial of Service | 会话清理遍历 list_dynamic() | accept | 动态工具数量预期 < 100，清理操作 O(n) 可接受；无性能风险 |
</threat_model>

<verification>
`python -c "from loopai.main import create_agent_components; from loopai.api.routes.control import router; from loopai.tools.prompt_builder import build_system_prompt; print('Integration OK')"`
</verification>

<success_criteria>
generate_tool 在 Agent 会话中可调用，confirm-tool-creation REST 端点处理用户确认/拒绝，system prompt 注入动态工具列表，会话关闭时清理会话级动态工具
</success_criteria>

<output>
After completion, create `.planning/phases/08-dynamic-tool-core/08-04-SUMMARY.md`
</output>
