---
phase: 07-chat-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loopai/session/context.py
  - src/loopai/state_machine/fsm.py
  - src/loopai/events/schemas.py
  - src/loopai/api/schemas.py
  - src/loopai/api/routes/control.py
  - src/loopai/api/app.py
autonomous: true
requirements: [CHAT-01, CHAT-02]
user_setup: []

must_haves:
  truths:
    - "Agent 完成一轮回复后状态为 FINISH_WAIT，不会终止会话"
    - "用户可以通过 POST /api/sessions/{id}/messages 发送新消息"
    - "新消息触发 FSM 从 FINISH_WAIT 转换回 REASON 继续处理"
    - "前端 SSE 长连接在整个会话生命周期中保持不断"
    - "Session 在多轮对话中持续累积消息"
  artifacts:
    - path: "src/loopai/session/context.py"
      provides: "FINISH_WAIT 状态枚举"
      contains: "FINISH_WAIT = 'finish_wait'"
    - path: "src/loopai/state_machine/fsm.py"
      provides: "多轮对话 FSM 循环（FINISH_WAIT 支持）"
      contains: "AgentState.FINISH_WAIT"
    - path: "src/loopai/events/schemas.py"
      provides: "user_message 和 round_end 事件模型"
      contains: "class UserMessage"
    - path: "src/loopai/api/schemas.py"
      provides: "SendMessageRequest / SendMessageResponse"
      contains: "class SendMessageRequest"
    - path: "src/loopai/api/routes/control.py"
      provides: "POST /api/sessions/{id}/messages 端点 + 多轮 _run_and_cleanup"
      contains: "\"/sessions/{session_id}/messages\""
  key_links:
    - from: "control.py POST /messages"
      to: "asyncio.Queue per session in app.state"
      via: "await app.state.session_queues[session_id].put(body.content)"
    - from: "fsm.run()"
      to: "control.py _run_and_cleanup loop"
      via: "while session.state != AgentState.ERROR calls fsm.run() repeatedly"
    - from: "fsm._handle_reason"
      to: "AgentState.FINISH_WAIT"
      via: "transition to FINISH_WAIT instead of FINISH"
---

<objective>
后端改造：为 FSM 添加 FINISH_WAIT 状态、使 Session 支持多轮对话、新增消息发送 API。

Purpose: 将 loopAI 从"发任务→等完成"的单轮模式改造为"对话→回复→再对话"的多轮聊天模式。
Output:
- AgentState.FINISH_WAIT 枚举值
- FSM.run() 支持多轮转换（在 FINISH_WAIT 时等待新消息继续）
- round_end / user_message 事件类型
- POST /api/sessions/{id}/messages 端点
- POST /api/sessions/{id}/stop 端点
- 基于 asyncio.Queue 的进程内消息传递机制
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-mode/07-CONTEXT.md
@src/loopai/session/context.py
@src/loopai/state_machine/fsm.py
@src/loopai/events/schemas.py
@src/loopai/api/schemas.py
@src/loopai/api/routes/control.py
@src/loopai/api/app.py
@src/loopai/main.py
@frontend/src/lib/eventTypes.ts
</context>

<interfaces>
From src/loopai/session/context.py:
```python
class AgentState(str, Enum):
    REASON = "reason"
    ACT = "act"
    OBSERVE = "observe"
    FINISH = "finish"
    ERROR = "error"
```

From src/loopai/state_machine/fsm.py:
```python
class ReActFSM:
    async def run(self, session: Session) -> Session:
        """while loop exits on FINISH or ERROR"""
```

From src/loopai/api/routes/control.py:
```python
async def _run_and_cleanup(session, fsm, bus, app):
    """Runs fsm.run(), marks completed in app.state.active_sessions"""
    
async def start_session(body, request):
    """POST /api/sessions/start — creates session, starts FSM background task"""

async def confirm_session(session_id, body, request):
    """POST /api/sessions/{id}/confirm — dangerous command confirmation"""
```

From src/loopai/api/app.py:
```python
app.state.bus = EventBus()
app.state.active_sessions: dict = {}
```

From src/loopai/events/schemas.py - existing Event types:
```python
StepStart, StepEnd, SessionEnd, LLMToken, ..., AgentCallStart, AgentCallEnd
Event union type with discriminator="event_type"
```

From src/loopai/api/schemas.py:
```python
class StartSessionRequest(BaseModel):
    prompt: str
    max_steps: int = 15

class StartSessionResponse(BaseModel):
    session_id: str

class ConfirmRequest(BaseModel):
    confirmation_id: str
    approved: bool
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 添加 FINISH_WAIT 状态 + FSM 多轮循环 + 新事件类型</name>
  <files>
    src/loopai/session/context.py
    src/loopai/state_machine/fsm.py
    src/loopai/events/schemas.py
    frontend/src/lib/eventTypes.ts
  </files>
  <action>
    Per D-02: FSM 新增 FINISH_WAIT 状态，收到新消息后回到 REASON。

    **1. AgentState 枚举 (context.py)**
    在 AgentState 枚举中 `FINISH` 和 `ERROR` 之间插入 `FINISH_WAIT = "finish_wait"`。
    不要修改其他值或重新排序已存在的值。

    **2. 事件模型 (events/schemas.py)**
    在 `SessionEnd` 类之后（Agent-as-Tool 事件之前）添加两个新事件类：

    ```python
    class UserMessage(EventBase):
        """当用户通过 POST /messages 发送消息时发布，用于前端轮次分组。"""
        event_type: Literal["user_message"] = "user_message"
        round_num: int
        content: str

    class RoundEnd(EventBase):
        """Agent 完成一轮对话（进入 FINISH_WAIT）时发布。"""
        event_type: Literal["round_end"] = "round_end"
        round_num: int
        total_steps: int
        token_usage: dict | None = None
    ```

    将 `UserMessage` 和 `RoundEnd` 加入 `Event` 联合类型（discriminated union）。按字母序插入到现有类型列表中。

    **3. TypeScript 事件类型 (frontend/src/lib/eventTypes.ts)**
    在 `SessionEndEvent` 之后添加两个新接口：

    ```typescript
    export interface UserMessageEvent extends EventBase {
      event_type: "user_message";
      round_num: number;
      content: string;
    }

    export interface RoundEndEvent extends EventBase {
      event_type: "round_end";
      round_num: number;
      total_steps: number;
      token_usage?: TokenUsage | null;
    }
    ```

    将 `UserMessageEvent` 和 `RoundEndEvent` 加入 `Event` 联合类型（按字母序插入）。更新 `EVENT_TYPE_MAP` 添加对应标签。

    **4. FSM 多轮改造 (fsm.py)**

    4a. 在 `run()` 方法中：
    - 将 while 循环条件从 `while session.state not in (AgentState.FINISH, AgentState.ERROR):` 改为 `while session.state not in (AgentState.FINISH, AgentState.FINISH_WAIT, AgentState.ERROR):`
    - session_end 事件发布改为条件性：`if session.state != AgentState.FINISH_WAIT:` 时发布，FINISH_WAIT 时不发布
    - 当 state 为 FINISH_WAIT 时，发布 round_end 事件（包含本轮步数和 token 用量统计），不回传 session（直接 return session）

    4b. 在 `_handle_reason()` 方法中：
    - 凡是设置 `session.state = AgentState.FINISH` 的地方，全部改为 `session.state = AgentState.FINISH_WAIT`（per D-02：收到新消息后回到 REASON）
    - budget exhausted (`action == "final"`) 的情况也改为 FINISH_WAIT（用户可通过新消息继续）

    4c. session_end 事件移动/调整：
    - `run()` 方法末尾的 `await self.bus.publish("session_end", ...)` 包装在 `if session.state != AgentState.FINISH_WAIT:` 条件中
    - ERROR 状态的 session_end 正常发布
    - 不需要新增其他 import

    4d. 需要添加的统计：
    在 FSM 的 `__init__` 中添加 `self._round_num: int = 0` 和 `self._turn_token_usage: dict | None = None` 用于 tracking。
    在 `run()` 中、while 循环之前每个 round 开始时递增 `self._round_num`。
    round_end 事件中包含 `round_num`、`total_steps`（本轮步数）和 `token_usage`（本轮累计）。

    **注意**: 不要修改 FSM 的构造函数签名或 `__init__` 参数。不要修改任何现有守卫或工具管线的逻辑。
    不要修改 `_handle_act` 或 `_handle_observe`。Do NOT add `chat_mode` parameter.

    **5. 验证**
    确保以下行为不变：
    - CLI 路径：FSM 返回 FINISH_WAIT 替代 FINISH，但 `run_session()` 不受影响（它只检查 session 对象）
    - 现有事件类型不受影响（只新增，不修改）
  </action>
  <verify>
    <automated>cd /home/atis/HE/loopAI && python -c "from loopai.session.context import AgentState; assert AgentState.FINISH_WAIT.value == 'finish_wait'; print('OK')"</automated>
    <automated>grep -c "FINISH_WAIT" src/loopai/state_machine/fsm.py || echo "MISSING"</automated>
    <automated>grep -c "class UserMessage" src/loopai/events/schemas.py || echo "MISSING"</automated>
    <automated>grep -c "class RoundEnd" src/loopai/events/schemas.py || echo "MISSING"</automated>
    <automated>grep -c "user_message" frontend/src/lib/eventTypes.ts || echo "MISSING"</automated>
    <automated>grep -c "round_end" frontend/src/lib/eventTypes.ts || echo "MISSING"</automated>
  </verify>
  <done>
    AgentState.FINISH_WAIT 已定义，FSM.run() 在 FINISH_WAIT 时退出而不发布 session_end，
    round_end 事件在 FINISH_WAIT 时正确发布，Python 和 TypeScript 事件类型均已更新。
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /api/sessions/{id}/messages 端点 + 多轮 _run_and_cleanup 循环</name>
  <files>
    src/loopai/api/routes/control.py
    src/loopai/api/schemas.py
    src/loopai/api/app.py
  </files>
  <action>
    Per D-03: SSE 保持长连接，新消息通过 REST API 发送。

    **1. 新 Pydantic Schema (api/schemas.py)**
    在现有 schema 之后添加：

    ```python
    class SendMessageRequest(BaseModel):
        """POST /api/sessions/{id}/messages 的请求体。"""
        content: str

    class SendMessageResponse(BaseModel):
        """POST /api/sessions/{id}/messages 的响应。"""
        message: str = "Message queued"
        session_id: str
        round_num: int
    ```

    更新 `__all__` 导出列表。

    **2. App 生命周期增加 session_queues (app.py)**
    在 `lifespan` 函数的 `app.state.active_sessions: dict = {}` 后添加：
    ```python
    app.state.session_queues: dict[str, asyncio.Queue] = {}
    ```
    在 yield 后的清理中：
    ```python
    for queue in app.state.session_queues.values():
        await queue.put(None)  # 发送关闭信号
    app.state.session_queues.clear()
    ```

    需要在文件顶部添加 `import asyncio`。

    **3. 重写 _run_and_cleanup 为多轮循环 (control.py)**

    将现有的 `_run_and_cleanup` 函数替换为支持多轮对话的版本（保留原有的异常处理架构，per D-01）：

    ```python
    async def _run_and_cleanup(session, fsm, bus, app):
        """多轮对话运行器。
        
        循环调用 fsm.run()，每次运行到 FINISH_WAIT 后通过 asyncio.Queue
        等待新消息。收到新消息后添加回 Session 并继续下一轮。
        收到 None 关闭信号或 ERROR 时终止并发布 session_end。
        """
        session_id = session.session_id
        try:
            while True:
                try:
                    await fsm.run(session)
                except Exception as exc:
                    logger.error(
                        "Agent session %s failed: %s: %s",
                        session_id, type(exc).__name__, exc,
                    )
                    await bus.publish("error", {
                        "event_type": "error",
                        "session_id": session_id,
                        "error_type": type(exc).__name__,
                        "error_message": str(exc),
                        "step_num": session.step_count,
                    })
                    break

                if session.state == AgentState.ERROR:
                    break

                if session.state == AgentState.FINISH_WAIT:
                    # 等待下一条用户消息
                    queue = app.state.session_queues.get(session_id)
                    if queue is None:
                        break
                    new_message = await queue.get()
                    
                    if new_message is None:
                        # 关闭信号 —— 结束会话
                        break
                    
                    # 添加用户消息并继续循环
                    session.add_message("user", content=new_message)
                    session.state = AgentState.REASON

                if session.state == AgentState.FINISH:
                    break

        finally:
            # 发布 session_end
            await bus.publish("session_end", {
                "event_type": "session_end",
                "session_id": session_id,
                "final_state": session.state.value,
                "total_steps": session.step_count,
                "exit_reason": "chat_ended",
            })
            
            # 清理
            if session_id in app.state.active_sessions:
                entry = app.state.active_sessions[session_id]
                entry["status"] = "completed"
            app.state.session_queues.pop(session_id, None)
    ```

    需要从 `loopai.session.context import AgentState`（确保已 import 或添加 import）。

    **4. 修改 start_session 端点**
    - 创建 Queue 并注册到 `app.state.session_queues[session.session_id]`（在启动 FSM 任务之前）
    - 移除或注释掉 `clearPendingConfirmation` 相关的前端引用（js侧的逻辑不在后端处理）

    **5. 新增 POST /api/sessions/{session_id}/messages 端点**

    ```python
    @router.post("/sessions/{session_id}/messages")
    async def send_message(session_id: str, body: SendMessageRequest, request: Request):
        """向活跃会话发送新消息。
        
        将消息内容放入会话的 asyncio.Queue，FSM 的 _run_and_cleanup 
        循环从中取出后处理。同时发布 user_message 事件供 SSE 推送到前端。
        
        Args:
            session_id: 目标会话 ID。
            body: 包含 content 字段的请求体。
            request: FastAPI 请求对象。
        
        Returns:
            SendMessageResponse 包含 session_id 和 round_num。
            如果会话未找到或已结束返回 404。
        """
        from loopai.session.context import AgentState
        
        active = request.app.state.active_sessions
        queues = request.app.state.session_queues
        
        if session_id not in active:
            raise HTTPException(status_code=404, detail=f"Session '{session_id}' not found")
        
        entry = active[session_id]
        session = entry["session"]
        
        # 只允许在 FINISH_WAIT 或初始 REASON 状态下发送消息
        if session.state not in (AgentState.FINISH_WAIT, AgentState.REASON):
            raise HTTPException(
                status_code=409,
                detail=f"Session is in state '{session.state.value}', cannot accept messages"
            )
        
        if session_id not in queues:
            raise HTTPException(
                status_code=404,
                detail=f"No message queue for session '{session_id}'"
            )
        
        # 发布 user_message 事件供 SSE 推流
        round_num = getattr(entry.get("fsm"), "_round_num", 0) + 1  # 下一轮的编号
        await request.app.state.bus.publish("user_message", {
            "event_type": "user_message",
            "session_id": session_id,
            "round_num": round_num,
            "content": body.content,
        })
        
        # 放入队列 — _run_and_cleanup 会取出并处理
        await queues[session_id].put(body.content)
        
        return SendMessageResponse(
            session_id=session_id,
            round_num=round_num,
        )
    ```

    需要 import `SendMessageRequest` 和 `SendMessageResponse`（在文件顶部或函数内 import 避免循环依赖）。如果已 import `from loopai.api.schemas import StartSessionRequest, StartSessionResponse, ConfirmRequest`，添加这两个新类型。

    **6. 新增 POST /api/sessions/{session_id}/stop 端点**
    允许前端主动结束会话（关闭页面时调用）：

    ```python
    @router.post("/sessions/{session_id}/stop")
    async def stop_session(session_id: str, request: Request):
        """结束一个活跃会话。
        
        向会话消息队列发送 None 关闭信号，_run_and_cleanup 收到后
        发布 session_end 并清理资源。
        """
        queues = request.app.state.session_queues
        if session_id not in queues:
            raise HTTPException(status_code=404, detail=f"Session '{session_id}' not found or already ended")
        
        await queues[session_id].put(None)
        
        return {"session_id": session_id, "stopped": True}
    ```

    **7. 注册和路由**
    确保 control.py 已包含 `from loopai.api.schemas import SendMessageRequest, SendMessageResponse`。
    不需要修改 app.py 的路由挂载逻辑 `app.include_router(control.router, prefix="/api")` 已经覆盖。

    **8. 导入检查**
    确保 control.py 顶部有 `from loopai.session.context import AgentState`（或需要时从内部 import），
    以及有 `import asyncio`。
  </action>
  <verify>
    <automated>grep -c "class SendMessageRequest" src/loopai/api/schemas.py || echo "MISSING"</automated>
    <automated>grep -c "class SendMessageResponse" src/loopai/api/schemas.py || echo "MISSING"</automated>
    <automated>grep -c "session_queues" src/loopai/api/app.py || echo "MISSING"</automated>
    <automated>grep -c "send_message" src/loopai/api/routes/control.py || echo "MISSING"</automated>
    <automated>grep -c "stop_session" src/loopai/api/routes/control.py || echo "MISSING"</automated>
    <automated>grep -c "AgentState.FINISH_WAIT" src/loopai/api/routes/control.py || echo "MISSING"</automated>
  </verify>
  <done>
    用户可以通过 POST /api/sessions/{id}/messages {content: "..."} 向活跃会话发送消息，
    FSM 自动从 FINISH_WAIT 回到 REASON 继续处理，SSE 长连接保持不断。
    用户可以通过 POST /api/sessions/{id}/stop 主动结束会话。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->POST /messages | Untrusted user input crosses here |
| client->POST /stop | Session termination must be authorized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | S | POST /messages | mitigate | Validate content is non-empty string, reject non-str types (Pydantic handles this) |
| T-07-02 | E | POST /messages on ended session | mitigate | Check session is in FINISH_WAIT or REASON state; return 409 if not |
| T-07-03 | E | POST /stop on missing session | mitigate | Return 404 if session not found in queues |
| T-07-04 | E | SSE keeps streaming after session ends | mitigate | None sentinel on bus.shutdown() stops SSE; cleanup in finally block removes queue |
</threat_model>

<verification>
- AgentState.FINISH_WAIT defined in context.py
- FSM.run() publishes round_end instead of session_end when FINISH_WAIT
- POST /api/sessions/{id}/messages returns 200 with message queued
- POST /api/sessions/{id}/messages on active session triggers FSM continuation
- POST /api/sessions/{id}/messages on ended session returns 409
- POST /api/sessions/{id}/stop returns 200, session ends gracefully
- user_message event published via EventBus when message received
</verification>

<success_criteria>
- [ ] AgentState.FINISH_WAIT enum value exists
- [ ] FSM.run() loop excludes FINISH_WAIT from termination
- [ ] round_end event published on FINISH_WAIT transition
- [ ] user_message event published on new message
- [ ] POST /api/sessions/{id}/messages endpoint functional
- [ ] POST /api/sessions/{id}/stop endpoint functional
- [ ] _run_and_cleanup loops for multi-turn conversation
- [ ] TypeScript event types match Python schemas
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-mode/07-01-SUMMARY.md`
</output>
