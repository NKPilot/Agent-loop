---
phase: 09-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/loopai/events/schemas.py, frontend/src/lib/eventTypes.ts]
autonomous: true
requirements: [DYN-11, DYN-12, DYN-13, DYN-14]

must_haves:
  truths:
    - "sandbox_timeout 事件类已定义且包含 event_type/session_id/timestamp/step_num/tool_name/timeout_seconds 字段"
    - "sandbox_violation 事件类已定义且包含 violation_type (path_escape|network_attempt|sensitive_path) 和 detail 字段"
    - "sandbox_resource_exceeded 事件类已定义且包含 resource_type (memory|process|file_size) 和 limit/detail 字段"
    - "三种新事件类型已加入 Python Event 区分联合类型"
    - "TypeScript 前端事件类型已同步：3 个 interface + Event union + EVENT_TYPE_MAP"
  artifacts:
    - path: "src/loopai/events/schemas.py"
      provides: "3 个新事件类的 Pydantic 模型定义 + Event 联合类型更新"
      contains: "class SandboxTimeout"
      contains: "class SandboxViolation"
      contains: "class SandboxResourceExceeded"
    - path: "frontend/src/lib/eventTypes.ts"
      provides: "3 个新事件 interface + Event 联合类型 + EVENT_TYPE_MAP 条目"
      contains: "SandboxTimeoutEvent"
      contains: "SandboxViolationEvent"
      contains: "SandboxResourceExceededEvent"
  key_links:
    - from: "src/loopai/events/schemas.py Event 联合类型"
      to: "三个新事件类"
      via: "| SandboxTimeout | SandboxViolation | SandboxResourceExceeded"
      pattern: "SandboxTimeout.*SandboxViolation.*SandboxResourceExceeded"
    - from: "frontend/src/lib/eventTypes.ts Event 联合类型"
      to: "三个新 TypeScript interface"
      via: "| SandboxTimeoutEvent | SandboxViolationEvent | SandboxResourceExceededEvent"
      pattern: "SandboxTimeoutEvent.*SandboxViolationEvent.*SandboxResourceExceededEvent"
---

<objective>
为沙箱安全加固定义事件契约——新增 3 个沙箱违规事件类型，同步 Python Pydantic 模型和 TypeScript 前端接口。

Purpose: 后续 SandboxExecutor 加固（Plan 09-02）和 DynamicToolCreator 集成（Plan 09-03）需要这 3 个事件类型来发布审计事件。先定义契约，后续实现方可引用。

Output: Python schemas.py 新增 3 个 EventBase 子类 + Event 联合类型更新；TypeScript eventTypes.ts 新增 3 个 interface + Event union + EVENT_TYPE_MAP 更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/loopai/events/schemas.py
@frontend/src/lib/eventTypes.ts

<interfaces>
<!-- 现有 EventBase 基类模式——所有新事件类继承此模式 -->
From src/loopai/events/schemas.py:
```python
class EventBase(BaseModel):
    event_type: Literal[str]
    session_id: str
    timestamp: str = Field(
        default_factory=lambda: datetime.now(timezone.utc).isoformat()
    )

# 现有具体事件类示例（参考模式）:
class ToolCreationTestResult(EventBase):
    event_type: Literal["tool_creation_test_result"] = "tool_creation_test_result"
    tool_name: str
    status: Literal["passed", "failed", "timeout"]
    output: str
    error: str | None = None
    duration_ms: float
```

<!-- Event 联合类型末尾——新增事件追加到此列表中 -->
From src/loopai/events/schemas.py (lines 397-431):
```python
Event = Annotated[
    StepStart | StepEnd | ... | ToolCreationFailed,
    Field(discriminator="event_type"),
]
```

<!-- TypeScript EventBase + Event union 模式 -->
From frontend/src/lib/eventTypes.ts:
```typescript
export interface EventBase {
  event_type: string;
  session_id: string;
  timestamp: string;
}

export type Event =
  | StepStartEvent | StepEndEvent | ... | ToolCreationFailedEvent;

export const EVENT_TYPE_MAP: Record<string, string> = {
  // ... existing entries
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 3 个沙箱事件 Pydantic 模型 + 更新 Event 联合类型</name>
  <files>src/loopai/events/schemas.py</files>
  <behavior>
    - Test 1: SandboxTimeout 实例化后 event_type 为 "sandbox_timeout"，包含 step_num(int)、tool_name(str)、timeout_seconds(float)
    - Test 2: SandboxViolation 实例化后 event_type 为 "sandbox_violation"，violation_type 仅接受 "path_escape"|"network_attempt"|"sensitive_path"
    - Test 3: SandboxResourceExceeded 实例化后 event_type 为 "sandbox_resource_exceeded"，resource_type 仅接受 "memory"|"process"|"file_size"
    - Test 4: 三个新事件类均可通过 Event 区分联合类型反序列化（event_type discriminator 路由正确）
    - Test 5: model_dump() 输出 JSON 可序列化，timestamp 自动填充为 UTC ISO 格式字符串
  </behavior>
  <action>
在 schemas.py 文件末尾（`# ── 区分联合类型 ──` 注释之前）新增 `# ── 沙箱安全事件（Phase 9）──` 区块，包含以下 3 个事件类：

**SandboxTimeout** — 沙箱执行超时时发布。
- 字段：`event_type: Literal["sandbox_timeout"] = "sandbox_timeout"`、`step_num: int`、`tool_name: str`、`timeout_seconds: float`
- 超时意味着子进程被 SIGKILL 强制终止（subprocess.TimeoutExpired）

**SandboxViolation** — 沙箱违规访问时发布（越权路径 / 网络尝试 / 敏感路径）。
- 字段：`event_type: Literal["sandbox_violation"] = "sandbox_violation"`、`step_num: int`、`tool_name: str`、`violation_type: Literal["path_escape", "network_attempt", "sensitive_path"]`、`detail: str`
- `violation_type` 说明：
  - `path_escape` — 路径白名单校验失败（尝试访问沙箱外路径）
  - `network_attempt` — 尝试网络连接（被 unshare --net 阻断）
  - `sensitive_path` — 尝试访问敏感系统路径（/etc, /proc, ~/.ssh 等）

**SandboxResourceExceeded** — 沙箱资源超限时发布（内存 / fork / 文件大小）。
- 字段：`event_type: Literal["sandbox_resource_exceeded"] = "sandbox_resource_exceeded"`、`step_num: int`、`tool_name: str`、`resource_type: Literal["memory", "process", "file_size"]`、`limit: str`（如 "512MB", "0", "100MB"）、`detail: str`
- `resource_type` 说明：
  - `memory` — RLIMIT_AS 超限
  - `process` — RLIMIT_NPROC 阻止 fork
  - `file_size` — RLIMIT_FSIZE 阻止大文件写入

将三个新类追加到 `Event` 联合类型中（`ToolCreationFailed` 之后），保持 `Field(discriminator="event_type")` 不变。

确认导入：`EventBase`、`Literal`、`Field` 已在文件顶部可用，无需新增导入。
  </action>
  <verify>
    <automated>python -c "
from loopai.events.schemas import SandboxTimeout, SandboxViolation, SandboxResourceExceeded, Event
import json

# 测试实例化 + 序列化
e1 = SandboxTimeout(session_id='test', step_num=1, tool_name='test_tool', timeout_seconds=30.0)
assert e1.event_type == 'sandbox_timeout'
d1 = e1.model_dump()
assert d1['event_type'] == 'sandbox_timeout'
assert d1['timeout_seconds'] == 30.0

e2 = SandboxViolation(session_id='test', step_num=1, tool_name='test_tool', violation_type='network_attempt', detail='Network is unreachable')
assert e2.violation_type == 'network_attempt'

e3 = SandboxResourceExceeded(session_id='test', step_num=1, tool_name='test_tool', resource_type='memory', limit='512MB', detail='Memory limit exceeded')
assert e3.resource_type == 'memory'

# 测试 Event 联合类型反序列化
for e in [e1, e2, e3]:
    d = e.model_dump()
    restored = Event.model_validate(d)
    assert type(restored) is type(e)

print('ALL EVENT SCHEMA TESTS PASSED')
"</automated>
  </verify>
  <done>
- `SandboxTimeout` 类存在且包含所有字段，`event_type="sandbox_timeout"`
- `SandboxViolation` 类存在，`violation_type` 为 3 值 Literal
- `SandboxResourceExceeded` 类存在，`resource_type` 为 3 值 Literal
- 三种新类已加入 Event 联合类型
- 上述 Python 验证通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 同步 TypeScript 前端事件类型</name>
  <files>frontend/src/lib/eventTypes.ts</files>
  <action>
在 eventTypes.ts 中 `ToolCreationFailedEvent` 之后、`// ── Resilience events (Phase 4) ──` 之前新增 `// ── Sandbox security events (Phase 9) ──` 区块，包含 3 个 interface：

**SandboxTimeoutEvent** — `event_type: "sandbox_timeout"`、`step_num: number`、`tool_name: string`、`timeout_seconds: number`

**SandboxViolationEvent** — `event_type: "sandbox_violation"`、`step_num: number`、`tool_name: string`、`violation_type: "path_escape" | "network_attempt" | "sensitive_path"`、`detail: string`

**SandboxResourceExceededEvent** — `event_type: "sandbox_resource_exceeded"`、`step_num: number`、`tool_name: string`、`resource_type: "memory" | "process" | "file_size"`、`limit: string`、`detail: string`

将 3 个新 interface 追加到 `Event` 联合类型末尾（`ToolCreationFailedEvent` 之后）。

在 `EVENT_TYPE_MAP` 末尾添加 3 个中文标签：
- `sandbox_timeout: "Sandbox Timeout"`
- `sandbox_violation: "Sandbox Violation"`
- `sandbox_resource_exceeded: "Resource Exceeded"`

注意：这是纯类型同步——无前端 UI 变更，Phase 9 是纯后端阶段。
  </action>
  <verify>
    <automated>cd /home/he/workwork/Agent-loop/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
- `SandboxTimeoutEvent` interface 存在，字段与 Python 对应
- `SandboxViolationEvent` interface 存在，violation_type 为 3 值 union
- `SandboxResourceExceededEvent` interface 存在，resource_type 为 3 值 union
- 三种新类型已加入 Event 联合类型
- TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventBus (Python) → SSE Bridge → Frontend | 沙箱违规事件通过 EventBus 流式传输到前端；事件字段仅包含结构化诊断信息，不泄露系统路径或凭据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | SandboxViolation.detail | mitigate | `detail` 字段限定为人类可读的中文描述（如"路径 /tmp 不在白名单内"），不包含绝对文件系统和用户名信息；前端仅做展示不做解析 |
| T-09-02 | Information Disclosure | sandbox_resource_exceeded | accept | 资源限制值（512MB、0、100MB）为公开的安全策略配置，非敏感信息 |
</threat_model>

<verification>
# 事件 Schema 验证
python -c "
from loopai.events.schemas import SandboxTimeout, SandboxViolation, SandboxResourceExceeded, Event

# 验证 3 个事件类均可被 Event 联合类型反序列化
for cls in [SandboxTimeout, SandboxViolation, SandboxResourceExceeded]:
    e = cls(session_id='verify', step_num=10, tool_name='verify_tool',
            **({'timeout_seconds': 30.0} if cls is SandboxTimeout else
               {'violation_type': 'network_attempt', 'detail': 'test'} if cls is SandboxViolation else
               {'resource_type': 'memory', 'limit': '512MB', 'detail': 'test'}))
    assert Event.model_validate(e.model_dump())
print('VERIFIED')

# 验证 TypeScript 编译
cd frontend && npx tsc --noEmit
"
</verification>

<success_criteria>
- [ ] `SandboxTimeout` Pydantic 模型正确序列化/反序列化
- [ ] `SandboxViolation` Pydantic 模型正确序列化/反序列化，violation_type 约束生效
- [ ] `SandboxResourceExceeded` Pydantic 模型正确序列化/反序列化，resource_type 约束生效
- [ ] Event 区分联合类型可路由到三种新事件类型
- [ ] TypeScript 编译无错误，3 个新 interface 与 Python 字段一一对应
- [ ] EVENT_TYPE_MAP 包含 3 个新条目的中文标签
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-hardening/09-01-SUMMARY.md`
</output>
