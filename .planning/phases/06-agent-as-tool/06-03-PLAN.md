---
phase: 06-agent-as-tool
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/eventTypes.ts
  - frontend/src/components/AgentCallCard.tsx
  - frontend/src/components/StepCard.tsx
autonomous: true
requirements: [WEB-01, WEB-02]
---

<objective>
前端新增 agent_call_start/agent_call_end 事件类型、AgentCallCard 可展开嵌套卡片组件，展示多 Agent 调用链。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript 事件类型 + AgentCallCard 组件</name>
  <files>frontend/src/lib/eventTypes.ts, frontend/src/components/AgentCallCard.tsx</files>
  <read_first>
    - frontend/src/lib/eventTypes.ts (现有事件类型)
    - frontend/src/components/StepCard.tsx (嵌入位置)
    - .planning/phases/06-agent-as-tool/06-CONTEXT.md (WEB-01, WEB-02)
  </read_first>
  <action>
    **eventTypes.ts 修改：**
    - 添加 AgentCallStartEvent 接口: {event_type: "agent_call_start", session_id, step_num, agent_name, child_session_id, tool_call_id}
    - 添加 AgentCallEndEvent 接口: {event_type: "agent_call_end", session_id, step_num, agent_name, child_session_id, summary, tool_calls_count, token_usage, steps, success}
    - 更新 Event 联合类型和 EVENT_TYPE_MAP

    **AgentCallCard.tsx 创建：**
    - Props: { events: Event[], childSessionId: string }
    - 默认状态：显示紫色边框卡片，agent 名称 + 状态标记
    - 可展开：点击展开后 REST 获取子会话详情（/api/sessions/{child_session_id}），渲染嵌套时间线
    - 摘要栏：tool_calls 数量、token 消耗、执行步骤数、成功/失败状态
    - 空状态：子会话数据未就绪时的加载态
  </action>
  <acceptance_criteria>
    - AgentCallStartEvent/AgentCallEndEvent 类型正确加入 Event 联合类型
    - AgentCallCard 组件渲染 agent 名称和状态
    - 展开后显示子会话摘要（tool calls, tokens, steps）
    - 组件不抛运行时错误
  </acceptance_criteria>
  <done>AgentCallCard 组件可用</done>
</task>

<task type="auto">
  <name>Task 2: StepCard 集成 AgentCallCard</name>
  <files>frontend/src/components/StepCard.tsx</files>
  <read_first>
    - frontend/src/components/StepCard.tsx (现有 StepCard)
    - frontend/src/components/AgentCallCard.tsx (新组件)
  </read_first>
  <action>
    **StepCard.tsx 修改：**
    - 在 ACT 步骤的事件列表中检测 agent_call_start 事件
    - 当检测到时，在工具调用卡片区域渲染 AgentCallCard（紫色主题区别于普通工具调用）
    - AgentCallCard 接收该步骤的所有 agent_call_start/agent_call_end 事件
    - 嵌套卡片在普通工具调用卡片上方展示
  </action>
  <acceptance_criteria>
    - StepCard 正确检测 agent_call_start 事件
    - AgentCallCard 在 ACT 步骤中渲染
    - 普通工具调用卡片不受影响
    - 视觉上区分 Agent 调用（紫色）和普通工具调用（默认色）
  </acceptance_criteria>
  <done>前端多 Agent 调用链可视化完成</done>
</task>

</tasks>
