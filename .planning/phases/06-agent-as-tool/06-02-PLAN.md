---
phase: 06-agent-as-tool
plan: 02
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/loopai/agents/disk_agents.py
  - src/loopai/main.py
  - tests/test_agents.py
autonomous: true
requirements: [AGT-04, BIZ-03]
---

<objective>
创建磁盘诊断子 Agent + 集成到 create_agent_components()，完成 BIZ-03 多 Agent 磁盘诊断端到端验证。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 磁盘诊断子 Agent 定义</name>
  <files>src/loopai/agents/disk_agents.py</files>
  <read_first>
    - src/loopai/agents/decorator.py (@agent 装饰器)
    - src/loopai/agents/types.py (AgentMetadata)
    - src/loopai/tools/disk_tools.py (register_disk_tools)
    - .planning/phases/06-agent-as-tool/06-CONTEXT.md (D-03 独立工具集)
  </read_first>
  <action>
    创建 disk_agents.py，定义两个子 Agent：

    **disk_analyzer Agent:**
    - @agent(name="disk_analyzer", description="分析磁盘使用情况，找出大文件和可清理项", tools=[disk_df, disk_du, disk_find], max_steps=10)
    - system_prompt: "你是一个磁盘分析专家。使用工具扫描磁盘，找出占用空间大的目录和文件，分析哪些可以安全清理、哪些建议保留。输出结构化的分析报告。"
    - 工具集仅包含只读诊断工具（SAFE）

    **disk_cleaner Agent:**
    - @agent(name="disk_cleaner", description="执行磁盘清理——删除指定的文件或目录", tools=[disk_rm], max_steps=5)
    - system_prompt: "你是一个磁盘清理专家。接收需要清理的文件/目录列表，调用 disk_rm 执行删除。注意：disk_rm 是危险操作，系统会自动弹出确认框。"
    - 工具集仅包含 disk_rm（DANGEROUS——子 Agent 内的 PermissionGuard 行为由 Claude's Discretion 决定）
  </action>
  <acceptance_criteria>
    - disk_analyzer 和 disk_cleaner 两个 @agent 正确创建 AgentMetadata
    - disk_analyzer 工具集只有 disk_df/disk_du/disk_find（只读）
    - disk_cleaner 工具集只有 disk_rm
    - 文件可导入：from loopai.agents.disk_agents import disk_analyzer, disk_cleaner
  </acceptance_criteria>
  <done>磁盘诊断子 Agent 定义完成</done>
</task>

<task type="auto">
  <name>Task 2: create_agent_components 集成 + BIZ-03 测试</name>
  <files>src/loopai/main.py, tests/test_agents.py</files>
  <read_first>
    - src/loopai/main.py (create_agent_components)
    - src/loopai/agents/disk_agents.py (子 Agent 定义)
    - src/loopai/agents/tool.py (AgentTool)
    - src/loopai/agents/registry.py (AgentRegistry)
  </read_first>
  <action>
    **main.py 修改：**
    - 在 create_agent_components() 中添加 AgentRegistry 创建
    - 注册 disk_analyzer 和 disk_cleaner
    - 为每个注册的 Agent 创建 AgentTool，通过 registry.register_meta() 注册到主 ToolRegistry
    - 系统提示中提及可用的子 Agent

    **测试扩展：**
    - 在 tests/test_agents.py 中添加 BIZ-03 集成测试：
      1. 创建包含 AgentTool 的组件
      2. 验证 disk_analyzer AgentTool 在 ToolRegistry 中
      3. 验证 disk_cleaner AgentTool 在 ToolRegistry 中
      4. Mock 场景：主 Agent 先调用 disk_analyzer 获取诊断结果，再调用 disk_cleaner 执行清理
  </action>
  <acceptance_criteria>
    - create_agent_components() 返回的 registry 中包含 disk_analyzer 和 disk_cleaner AgentTool
    - 主 Agent 的 tool schemas 包含子 Agent 的 function 定义
    - BIZ-03 集成测试通过
    - uv run pytest tests/ -x 无回归
  </acceptance_criteria>
  <done>多 Agent 集成完成，BIZ-03 验证通过</done>
</task>

</tasks>
