---
phase: 10-ux-tool-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loopai/tools/types.py
  - src/loopai/tools/registry.py
  - src/loopai/tools/tool_persistence.py
  - src/loopai/tools/dynamic_creator.py
  - src/loopai/events/schemas.py
  - src/loopai/main.py
  - frontend/src/lib/eventTypes.ts
autonomous: true
requirements:
  - DYN-15
  - DYN-17
  - DYN-22
must_haves:
  truths:
    - "ToolMetadata 有 enabled 字段，默认 True"
    - "get_schemas() 不返回 enabled=False 的工具（自动过滤）"
    - "禁用状态通过 meta.json 跨重启持久化"
    - "系统启动时自动加载沙箱级和项目级动态工具到 ToolRegistry"
    - "新事件类型（tool_disabled/tool_enabled/tool_deleted/tool_updated）可用"
    - "ToolCreationRequested 事件携带 is_update 标记"
  artifacts:
    - path: "src/loopai/tools/types.py"
      provides: "ToolMetadata.enabled 字段"
      contains: "enabled: bool = True"
    - path: "src/loopai/tools/registry.py"
      provides: "get_schemas() 过滤 enabled=False"
      pattern: "m.enabled"
    - path: "src/loopai/tools/tool_persistence.py"
      provides: "meta.json enabled 字段读写"
      pattern: "enabled"
    - path: "src/loopai/tools/dynamic_creator.py"
      provides: "build_func_ref 静态方法，startup 注册加载从磁盘读取的工具"
    - path: "src/loopai/events/schemas.py"
      provides: "4 个新事件类型 + is_update 字段"
      pattern: "class ToolDisabled|class ToolEnabled|class ToolDeleted|class ToolUpdated|is_update"
    - path: "frontend/src/lib/eventTypes.ts"
      provides: "同步的 TypeScript 事件类型"
    - path: "src/loopai/main.py"
      provides: "启动时自动加载持久化工具"
      pattern: "load_sandbox_tools|load_project_tools"
  key_links:
    - from: "ToolRegistry.get_schemas()"
      to: "ToolMetadata.enabled"
      via: "过滤 enabled=False"
      pattern: "enabled"
    - from: "create_agent_components"
      to: "ToolPersistenceManager.load_*"
      via: "启动时扫描 .sandbox/tools/ 和 src/loopai/tools/dynamic/"
    - from: "schemas.py"
      to: "eventTypes.ts"
      via: "镜像同步"
---

<objective>
后端核心：为动态工具管理建立类型层基础设施。

Purpose:
- 为动态工具添加 enabled/disabled 状态字段
- 确保禁用工具不暴露给 LLM（get_schemas 过滤）
- 确保禁用状态跨重启持久化（meta.json）
- 新增工具管理事件类型供 JSONL 审计追踪
- 启动时自动加载持久化工具
- 提供 func_ref 重构能力（供 startup 加载从磁盘重建）

Output:
- ToolMetadata.enabled 字段
- ToolRegistry.get_schemas() enabled 过滤
- ToolPersistenceManager meta.json enabled 读写
- DynamicToolCreator.build_func_ref 静态方法
- 4 个新事件类型 + is_update 字段 + TypeScript 镜像
- create_agent_components startup 加载
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/10-CONTEXT.md  — D-03, D-04, D-06, D-08, D-22
@.planning/REQUIREMENTS.md — DYN-15, DYN-17, DYN-22
</execution_context>

<context>
@src/loopai/tools/types.py
@src/loopai/tools/registry.py
@src/loopai/tools/tool_persistence.py
@src/loopai/tools/dynamic_creator.py
@src/loopai/events/schemas.py
@src/loopai/main.py
@frontend/src/lib/eventTypes.ts

<interfaces>
<!-- Existing contracts that tasks in this plan modify -->

From src/loopai/tools/types.py - ToolMetadata current shape:
```python
class ToolMetadata(BaseModel):
    name: str
    description: str
    permission_level: PermissionLevel = PermissionLevel.SAFE
    timeout: float = 30.0
    retry: RetryConfig = RetryConfig()
    tags: list[str] = []
    param_schema: dict = {}
    is_dynamic: bool = False
    func_ref: Any = Field(default=None, exclude=True)
    validation_model: Any = Field(default=None, exclude=True)
```

From src/loopai/tools/registry.py - ToolRegistry.get_schemas():
```python
def get_schemas(self, exclude_open: set[str] | None = None) -> list[dict]:
    static_tools = list(self._static_tools.values())
    dynamic_tools = list(self._dynamic_tools.values())
    tools = static_tools + dynamic_tools
    if exclude_open:
        tools = [m for m in tools if m.name not in exclude_open]
    return [meta.to_openai_schema() for meta in tools]
```

From src/loopai/tools/tool_persistence.py - ToolPersistenceManager._build_meta_dict():
```python
@staticmethod
def _build_meta_dict(tool_name, language, persistence, extra):
    return {
        "name": extra.get("name", tool_name),
        "description": extra.get("description", ""),
        "language": language,
        "param_schema": extra.get("param_schema", {}),
        "created_at": extra.get("created_at", ...),
        "persistence": persistence,
    }
```

From src/loopai/events/schemas.py - Event union currently has 32 event types,
ending with SandboxResourceExceeded.

From src/loopai/tools/dynamic_creator.py - _make_func_ref closure:
```python
def _make_func_ref(self, tool_id, code, language, config):
    async def _dynamic_tool_func(**kwargs):
        sandbox = SandboxExecutor(timeout=30.0, event_bus=None)
        os.makedirs(".sandbox", exist_ok=True)
        with tempfile.TemporaryDirectory(prefix="tool_run_", dir=".sandbox") as tmpdir:
            ...  # uses code, language, tmpdir
    return _dynamic_tool_func
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加 ToolMetadata.enabled 字段 + ToolRegistry enabled 过滤 + prompt_builder 兼容</name>

  <files>
    src/loopai/tools/types.py
    src/loopai/tools/registry.py
  </files>

  <read_first>
    @src/loopai/tools/types.py (full file — add enabled after is_dynamic)
    @src/loopai/tools/registry.py (full file — modify get_schemas filtering)
  </read_first>

  <action>
    1. In `src/loopai/tools/types.py`, add `enabled: bool = True` field to `ToolMetadata` class, placed between `is_dynamic` and `func_ref`.

    2. In `src/loopai/tools/registry.py`, modify `get_schemas()` to filter out tools where `enabled=False`. The filtering applies to both static and dynamic partitions (static tools default to True, so no behavioral change). Methods `list_all()` and `list_dynamic()` are NOT filtered — they return all tools including disabled ones (needed by management panel).

    The modified get_schemas logic:
    ```
    static_tools = list(self._static_tools.values())
    dynamic_tools = [m for m in self._dynamic_tools.values() if m.enabled]
    tools = static_tools + dynamic_tools
    (rest unchanged)
    ```

    Do NOT modify `prompt_builder.py`. It already calls `registry.get_schemas()` to build the tool list, so it automatically inherits the enabled filtering. Per D-03, this is the single entry point.
  </action>

  <verify>
    <automated>cd /home/atis/HE/loopAI && python -c "
from loopai.tools.types import ToolMetadata
m = ToolMetadata(name='test', description='test')
assert m.enabled == True, 'enabled default should be True'
m2 = ToolMetadata(name='test2', description='test', enabled=False)
assert m2.enabled == False, 'enabled should be settable'
print('ToolMetadata.enabled OK')
"</automated>
  </verify>

  <acceptance_criteria>
    1. ToolMetadata 新增 `enabled: bool = True` 字段, 序列化/反序列化工作正常
    2. `ToolRegistry.get_schemas()` 返回的列表中不包含 `enabled=False` 的工具
    3. `ToolRegistry.list_all()` 和 `list_dynamic()` 不受 enabled 影响 —— 返回全部工具
    4. 新建 ToolMetadata 实例时 enabled 默认 True（向后兼容）
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ToolPersistenceManager meta.json enabled 读写 + DynamicToolCreator.build_func_ref 静态方法</name>

  <files>
    src/loopai/tools/tool_persistence.py
    src/loopai/tools/dynamic_creator.py
  </files>

  <read_first>
    @src/loopai/tools/tool_persistence.py (full file — modify _build_meta_dict, load_sandbox_tools, load_project_tools)
    @src/loopai/tools/dynamic_creator.py (full file — add build_func_ref staticmethod)
  </read_first>

  <action>
    1. In `src/loopai/tools/tool_persistence.py`:
       - In `_build_meta_dict()`: add `"enabled": extra.get("enabled", True)` after `"persistence": persistence`
       - In `load_sandbox_tools()` and `load_project_tools()`: the meta dict already returns all meta.json content. No additional change needed — enabled is already included when reading meta.json.

    2. In `src/loopai/tools/dynamic_creator.py`:
       - Add a `@staticmethod` method `build_func_ref(code: str, language: str) -> Callable` that returns an async function with the same closure pattern as `_make_func_ref` (captures code and language, creates SandboxExecutor per call, handles .sandbox dir, builds wrapper with args.json). This static method exists so startup loading (create_agent_components) can reconstruct func_refs from persisted tool code without needing a DynamicToolCreator instance.
       - Refactor `_make_func_ref()` to delegate to `build_func_ref()` internally, keeping the same behavior for the generate_tool path. The `_make_func_ref` signature stays the same (captures tool_id and config for potential future use but delegates the closure creation).
  </action>

  <verify>
    <automated>cd /home/atis/HE/loopAI && python -c "
from loopai.tools.dynamic_creator import DynamicToolCreator
fn = DynamicToolCreator.build_func_ref('print(\"hello\")', 'python')
import inspect
assert inspect.iscoroutinefunction(fn), 'build_func_ref should return async function'
print('build_func_ref OK')

from loopai.tools.tool_persistence import ToolPersistenceManager
mgr = ToolPersistenceManager()
# Test _build_meta_dict includes enabled
import json
result = json.dumps(mgr._build_meta_dict('test_tool', 'python', 'sandbox', {'name': 'test'}))
assert '\"enabled\"' in result, 'enabled should be in meta_dict'
print('meta_dict enabled OK')
"</automated>
  </verify>

  <acceptance_criteria>
    1. `ToolPersistenceManager._build_meta_dict()` 输出包含 `"enabled": True`（默认）
    2. 现有 save/load 流程不受影响 —— 旧 meta.json 缺少 enabled 不影响读取
    3. `DynamicToolCreator.build_func_ref(code, language)` 返回 async 函数，行为与 _make_func_ref 一致
    4. `_make_func_ref()` 内部委托给 `build_func_ref()`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 新增 4 个工具管理事件 + is_update 字段 + TypeScript 镜像 + startup 加载</name>

  <files>
    src/loopai/events/schemas.py
    frontend/src/lib/eventTypes.ts
    src/loopai/main.py
  </files>

  <read_first>
    @src/loopai/events/schemas.py (full file — add 4 event classes after SandboxResourceExceeded, modify ToolCreationRequested)
    @frontend/src/lib/eventTypes.ts (full file — add 4 TS interfaces, update Event union, EVENT_TYPE_MAP)
    @src/loopai/main.py (full file — add startup loading after persistence creation)
  </read_first>

  <action>
    1. In `src/loopai/events/schemas.py`, after the `class SandboxResourceExceeded` block (line ~442), add 4 new event classes in a new section `# ── 工具管理事件（Phase 10）──`:

       - `ToolDisabled`: event_type "tool_disabled", fields: `tool_name: str`
       - `ToolEnabled`: event_type "tool_enabled", fields: `tool_name: str`
       - `ToolDeleted`: event_type "tool_deleted", fields: `tool_name: str`, `persistence: str`
       - `ToolUpdated`: event_type "tool_updated", fields: `tool_name: str`, `tool_id: str`, `old_tool_name: str`

       In `ToolCreationRequested`, add `is_update: bool = False` field after `param_schema`.

       Add all 4 new classes to the `Event` discriminated union type (after SandboxResourceExceeded). Update the class comment that says "32 个类型" to reflect the new count (36).

    2. In `frontend/src/lib/eventTypes.ts`:

       Add 4 new interfaces in a new section `// ── Tool management events (Phase 10) ──`:
       - `ToolDisabledEvent`: event_type "tool_disabled", fields matching Python
       - `ToolEnabledEvent`: event_type "tool_enabled"
       - `ToolDeletedEvent`: event_type "tool_deleted"
       - `ToolUpdatedEvent`: event_type "tool_updated"
       
       Add them to the `Event` discriminated union type.
       Add entries to `EVENT_TYPE_MAP` with Chinese labels.

       In `ToolCreationRequestedEvent`, add `is_update: boolean;` field.

    3. In `src/loopai/main.py`:

       In `create_agent_components()`, after the persistence manager is created (`tool_persistence = ToolPersistenceManager()`) and before `dynamic_creator = DynamicToolCreator(...)`, add startup loading of persisted tools:

       ```
       # 启动时自动加载持久化动态工具（DYN-22）
       for loader_name, loader_fn in [("sandbox", persistence.load_sandbox_tools), ("project", persistence.load_project_tools)]:
           for tool_data in loader_fn():
               meta_dict = tool_data.get("meta", {})
               tool_id = tool_data["tool_name"]
               code = tool_data["code"]
               language = tool_data["language"]
               persistence_level = tool_data["persistence"]
               enabled = meta_dict.get("enabled", True)

               tags = ["dynamic", f"lang:{language}", f"persist:{persistence_level}"]
               if not enabled:
                   tags.append("disabled")

               from loopai.tools.types import ToolMetadata, PermissionLevel
               import json
               meta = ToolMetadata(
                   name=tool_id,
                   description=meta_dict.get("description", ""),
                   permission_level=PermissionLevel.MODERATE,
                   timeout=30.0,
                   param_schema=meta_dict.get("param_schema", {}),
                   func_ref=DynamicToolCreator.build_func_ref(code, language),
                   is_dynamic=True,
                   enabled=enabled,
                   tags=tags,
                   code=code,
               )
               try:
                   registry.register_meta(meta, is_dynamic=True)
               except ValueError:
                   pass  # skip duplicates silently
       ```

       Add `import json` at the top of the function scope.

       Also add `code` to the ToolMetadata construction in `_stage56_persist_and_register()` method of DynamicToolCreator — when creating the ToolMetadata for a newly created tool, set `code=code` so the code is stored. Currently the code is only captured inside the func_ref closure.

       Wait — the `code` field in ToolMetadata needs to be added first. Let me add it in Task 1 or Task 2. Actually, I'll include it in Task 1: add `code: str = ""` field to ToolMetadata, placed after `enabled: bool = True`.

       Let me re-read: Task 1 adds `enabled: bool = True`. I'll also add `code: str = ""` in Task 1.

       So revision to Task 1: add both `enabled: bool = True` and `code: str = ""` to ToolMetadata. The `code` field is NOT excluded from serialization (it's needed for the management panel REST API to return it). But it IS excluded from `to_openai_schema()` output.

       In `ToolMetadata.to_openai_schema()`: the current implementation doesn't include code (it only returns name, description, parameters). No change needed.

    4. Add `"code"` to the meta_dict in DynamicToolCreator._stage56_persist_and_register():
       When constructing meta_dict for persistence, ensure code is included so it's stored either in the func_ref closure or as the actual code. Currently meta_dict doesn't include code — it's only captured in the closure. The code is already saved to disk separately (tool.py/tool.sh), so it's available for startup loading.
  </action>

  <verify>
    <automated>cd /home/atis/HE/loopAI && python -c "
from loopai.events.schemas import ToolDisabled, ToolEnabled, ToolDeleted, ToolUpdated, ToolCreationRequested, Event
# Verify new event classes exist
td = ToolDisabled(event_type='tool_disabled', session_id='s1', tool_name='test_tool')
te = ToolEnabled(event_type='tool_enabled', session_id='s1', tool_name='test_tool')
tde = ToolDeleted(event_type='tool_deleted', session_id='s1', tool_name='test_tool', persistence='sandbox')
tup = ToolUpdated(event_type='tool_updated', session_id='s1', tool_name='test_tool', tool_id='dynamic.abc_test', old_tool_name='test_tool')
assert td.tool_name == 'test_tool'
assert te.tool_name == 'test_tool'
assert tde.persistence == 'sandbox'
assert tup.old_tool_name == 'test_tool'

# Verify is_update on ToolCreationRequested
tcr = ToolCreationRequested(event_type='tool_creation_requested', session_id='s1', step_num=1, confirmation_id='abc', tool_name='t', tool_id='id', description='', code='', language='python', risk_flags=[], test_code='', param_schema={})
assert tcr.is_update == False, 'is_update defaults to False'

# Verify Event union includes new types
import typing
origin = typing.get_origin(Event)
args = typing.get_args(Event)
names = [a.__name__ for a in args]
for name in ['ToolDisabled', 'ToolEnabled', 'ToolDeleted', 'ToolUpdated']:
    assert name in names, f'{name} not in Event union'
print('Event types OK')
"</automated>
  </verify>

  <acceptance_criteria>
    1. 4 个新事件模型（ToolDisabled, ToolEnabled, ToolDeleted, ToolUpdated）存在于 schemas.py
    2. Event 联合类型包含所有 4 个新事件
    3. ToolCreationRequested 有 `is_update: bool = False` 字段
    4. TypeScript 端有对应的 4 个 interface + EVENT_TYPE_MAP 条目
    5. create_agent_components() 在启动时调用 load_sandbox_tools() + load_project_tools() 并将工具注册到 ToolRegistry
    6. ToolMetadata 有 `code: str = ""` 字段
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolRegistry -> get_schemas() | get_schemas() 输出直接送入 system prompt 和 LLM function calling |
| meta.json filesystem | 持久化存储含 enabled 字段，文件系统写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | ToolRegistry.get_schemas() | mitigate | 过滤 enabled=False 的 dynamic tools；静态工具依赖默认 True |
| T-10-02 | Information Disclosure | Event types | accept | 工具管理事件仅含名称和级别，不包含代码或内部路径。单用户本地工具，暴露面可接受 |
| T-10-03 | Tampering | meta.json enabled field | accept | 本地文件系统，非网络暴露。dynamic tools 本身就允许用户信任边界修改 |
</threat_model>

<verification>
- `python -c "from loopai.tools.types import ToolMetadata; m = ToolMetadata(name='t', description='d'); assert m.enabled == True"`
- `python -c "from loopai.events.schemas import ToolDisabled, ToolEnabled, ToolDeleted, ToolUpdated"`
- `python -c "import typing; args = typing.get_args(__import__('loopai.events.schemas').schemas.Event); names = [a.__name__ for a in args]; assert all(n in names for n in ['ToolDisabled', 'ToolEnabled', 'ToolDeleted', 'ToolUpdated'])"`
- `cd frontend && npx tsc --noEmit 2>&1 | head -20` (0 errors)
</verification>

<success_criteria>
1. ToolMetadata.enabled 默认 True，可序列化/反序列化
2. ToolRegistry.get_schemas() 不返回 disabled tools
3. ToolPersistenceManager meta.json 包含 enabled 字段
4. DynamicToolCreator.build_func_ref() 可独立构造 func_ref
5. 4 个新事件类型 + is_update 字段在所有层可用（Python + TypeScript）
6. create_agent_components 启动时加载持久化工具到 registry
</success_criteria>

<output>
After completion, create `.planning/phases/10-ux-tool-management/10-01-SUMMARY.md`
</output>
