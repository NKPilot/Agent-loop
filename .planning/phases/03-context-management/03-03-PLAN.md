---
phase: 03-context-management
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/loopai/state_machine/fsm.py
  - src/loopai/config.py
  - src/loopai/main.py
  - tests/test_fsm.py
autonomous: true
requirements: [CTX-02, CTX-03, CTX-04]
must_haves:
  truths:
    - "FSM._handle_reason 在 LLM 调用前执行 TokenGuard 检查，需要压缩时调用 ContextCompressor"
    - "压缩后的新消息列表注入 LLM 调用，发布 context_compacted 事件"
    - "FSM._handle_act 在工具输出 >80K 字符时用溢出文件引用替换上下文中完整内容"
    - "所有消息操作（包括压缩生成的新消息）均为追加式，不做原地修改"
  artifacts:
    - path: "src/loopai/state_machine/fsm.py"
      provides: "TokenGuard + ContextCompressor + OverflowFile 集成到 FSM"
      contains: "context_compacted"
  key_links:
    - from: "src/loopai/state_machine/fsm.py::_handle_reason"
      to: "src/loopai/state_machine/guards.py::TokenGuard"
      via: "pre-LLM-call token check"
      pattern: "token_guard"
    - from: "src/loopai/state_machine/fsm.py::_handle_reason"
      to: "src/loopai/context/compressor.py::ContextCompressor"
      via: "compression trigger when TokenGuard signals 'compress'"
      pattern: "compressor"
    - from: "src/loopai/state_machine/fsm.py::_handle_act"
      to: "src/loopai/tools/types.py::ToolResult.overflow_file"
      via: "tool result content replacement with overflow reference"
      pattern: "overflow_file"
---

<objective>
FSM 集成：TokenGuard + 压缩 + 溢出文件 + 追加式固化

Purpose: 将 Plan 01-02 的 TokenCounter、TokenGuard、ContextCompressor、溢出文件全部集成到 ReActFSM 中（CTX-02, CTX-03, CTX-04）。_handle_reason 在 LLM 调用前插入 TokenGuard 检查和压缩触发；_handle_act 在工具执行后处理溢出文件引用。追加式存储原则在此验证固化。

Output: 集成上下文的完整 ReActFSM
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-context-management/03-CONTEXT.md (Integration Points 和 D-01~D-04)

<!-- Source of truth files -->
@src/loopai/state_machine/fsm.py (全文 — 现有 _handle_reason 和 _handle_act 结构)
@src/loopai/config.py (AgentConfig 模式)
@src/loopai/main.py (FSM 创建和注入模式)
@tests/test_fsm.py (现有测试模式 — mock-based testing)

<!-- Prior plan SUMMARYs for FSM integration pattern -->
@.planning/phases/02-tool-system-biz-validation/02-03-SUMMARY.md (FSM 之前如何集成 ToolRegistry/ToolExecutor/PermissionGuard)

<interfaces>
From prior plans (interfaces the executor needs):

```python
# src/loopai/state_machine/guards.py — TokenGuard (Plan 02)
class TokenGuard:
    def __init__(self, token_counter: TokenCounter, window_size: int = 128000, threshold: float = 0.75): ...
    def check(self, messages: list[dict]) -> tuple[str, int, int]:
        """Returns (action, token_count, threshold_tokens).
        action: "ok" | "compress"
        """

# src/loopai/context/compressor.py — ContextCompressor (Plan 02)
class ContextCompressor:
    def __init__(self, token_counter: TokenCounter, window_size: int = 128000, threshold: float = 0.75, target: float = 0.50, preserve_rounds: int = 3): ...
    async def check_and_compress(self, messages: list[dict], summary_fn: Callable[[list[dict]], Awaitable[str]]) -> tuple[list[dict], bool, dict]:
        """Returns (compressed_messages, was_compressed, metadata)."""

# src/loopai/context/token_counter.py — TokenCounter (Plan 01)
class TokenCounter:
    def count_messages(self, messages: list[dict]) -> int: ...

# src/loopai/events/schemas.py — ContextCompacted event (Plan 01)
class ContextCompacted(EventBase):
    event_type: Literal["context_compacted"] = "context_compacted"
    step_num: int
    tokens_before: int
    tokens_after: int
    tokens_saved: int
    rounds_preserved: int
    summary_message_count: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FSM._handle_reason — TokenGuard + ContextCompressor 集成</name>
  <files>
    src/loopai/state_machine/fsm.py
    src/loopai/config.py
    src/loopai/main.py
    tests/test_fsm.py
  </files>
  <read_first>
    @src/loopai/state_machine/fsm.py (全文 — 理解 _handle_reason 的现有流程: validate → budget_check → LLM call → publish events)
    @src/loopai/state_machine/guards.py (TokenGuard interface from Plan 02)
    @src/loopai/context/compressor.py (ContextCompressor interface from Plan 02)
    @src/loopai/events/schemas.py (ContextCompacted event definition from Plan 01)
    @src/loopai/main.py (run_session — 现有 FSM 创建和参数注入模式)
    @tests/test_fsm.py (前 60 行 — helper 函数和 mock 模式)
    @.planning/phases/02-tool-system-biz-validation/02-03-SUMMARY.md (Task 2 展示了 FSM 如何新增参数和重构 _handle_act — 类似的模式)
  </read_first>
  <action>
    修改 `src/loopai/state_machine/fsm.py`：

    1. 在 `if TYPE_CHECKING` 块中新增导入：
       ```python
       from loopai.context.compressor import ContextCompressor
       from loopai.state_machine.guards import TokenGuard
       ```

    2. 在 `ReActFSM.__init__()` 签名中新增两个参数：
       ```python
       token_guard: TokenGuard | None = None,
       compressor: ContextCompressor | None = None,
       ```
       存储在 self 上：`self.token_guard` 和 `self.compressor`。

    3. 在 `_handle_reason()` 中，在 `self.message_validator.validate(session.messages)` 之后、`self.budget_guard.check(...)` 之前插入 TokenGuard 检查：
       ```python
       # TokenGuard: check token budget before LLM call (Phase 3)
       if self.token_guard is not None:
           action, token_count, threshold_tokens = self.token_guard.check(session.messages)
           if action == "compress" and self.compressor is not None:
               # Define summary_fn inline — calls LLM via a dedicated summary request
               async def _summary_fn(old_msgs: list[dict]) -> str:
                   summary_prompt = self.compressor._build_summary_prompt(old_msgs)
                   summary_response = await self.client.complete(
                       [{"role": "system", "content": summary_prompt}],
                       tools=None,
                       session_id=session.session_id,
                       step_num=step_num,
                   )
                   return summary_response.get("content", "")
               
               compressed, was_compressed, meta = await self.compressor.check_and_compress(
                   session.messages, _summary_fn
               )
               if was_compressed:
                   # Replace session messages with compressed version
                   session.messages.clear()
                   session.messages.extend(compressed)
                   
                   # Publish context_compacted event
                   await self.bus.publish(
                       "context_compacted",
                       {
                           "event_type": "context_compacted",
                           "session_id": session.session_id,
                           "step_num": step_num,
                           "tokens_before": meta["tokens_before"],
                           "tokens_after": meta["tokens_after"],
                           "tokens_saved": meta["tokens_saved"],
                           "rounds_preserved": meta.get("rounds_preserved", 0),
                           "summary_message_count": meta.get("summary_message_count", 0),
                       },
                   )
       ```

       注意：_summary_fn 使用 self.client.complete 进行摘要调用，但不传入 tools（摘要不需要工具）。响应只取 content 字段。这是计划内的额外 LLM 调用，消耗 token 用于生成高质量摘要。

    修改 `src/loopai/config.py`：

    4. 在 `AgentConfig` 类中新增字段：
       ```python
       context_window: int = 128000  # Default GPT-4o context window
       ```

    修改 `src/loopai/main.py`：

    5. 在 `run_session()` 中，创建 FSM 时传入 token_guard 和 compressor：
       ```python
       from loopai.context.token_counter import TokenCounter
       from loopai.context.compressor import ContextCompressor
       from loopai.state_machine.guards import TokenGuard
       
       # Phase 3: Context management
       token_counter = TokenCounter()
       token_guard = TokenGuard(token_counter, window_size=config.context_window)
       compressor = ContextCompressor(token_counter, window_size=config.context_window)
       
       fsm = ReActFSM(
           client=client,
           bus=bus,
           budget_guard=budget_guard,
           loop_detector=loop_detector,
           message_validator=message_validator,
           registry=registry,
           executor=executor,
           permission_guard=permission_guard,
           token_guard=token_guard,      # NEW
           compressor=compressor,         # NEW
       )
       ```

    修改 `tests/test_fsm.py`：

    6. 存根：在所有现有 `_create_phase1_fsm` 和直接构造 FSM 的位置，为新的 `token_guard` 和 `compressor` 参数传递 `None` 值以保持向后兼容。FSM 已有 `token_guard: TokenGuard | None = None` 参数默认值，所以只需检查哪些测试直接用 `ReActFSM(...)` 构造需要增加 kwargs。

    7. 新增测试类 `TestContextManagement`：
       - `test_token_guard_triggers_compression`：
         - mock TokenGuard.check() 返回 ("compress", 96000, 96000)
         - mock ContextCompressor.check_and_compress() 返回 (compressed_msgs, True, meta)
         - mock LLMClient.complete() 返回正常响应
         - 验证 session.messages 被替换为 compressed_msgs
         - 验证 context_compacted 事件被发布
       - `test_no_compression_below_threshold`：
         - mock TokenGuard.check() 返回 ("ok", 50000, 96000)
         - 验证 ContextCompressor.check_and_compress 未被调用
       - `test_summary_llm_call`：
         - 验证当需要压缩时，summary_fn 使用 self.client.complete 进行一次摘要 LLM 调用（无 tools 参数）
       - `test_compressor_none_skips_compression`：
         - token_guard 返回 "compress" 但 compressor 为 None → 不压缩，继续正常流程
  </action>
  <verify>
    <automated>cd /home/atis/HE/loopAI && python -m pytest tests/test_fsm.py -x -v -k "TestContextManagement or compression or token_guard" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TokenGuard.check() 返回 "compress" 时触发 ContextCompressor
    - 压缩后 session.messages 替换为压缩版本
    - context_compacted 事件被发布到 EventBus
    - TokenGuard 返回 "ok" 时不触发压缩
    - compressor 为 None 时即使信号为 "compress" 也不触发压缩
  </acceptance_criteria>
  <done>
    FSM._handle_reason 集成 TokenGuard + ContextCompressor，通过全部测试
  </done>
</task>

<task type="auto">
  <name>Task 2: FSM._handle_act 溢出文件引用 + 追加式固化验证</name>
  <files>
    src/loopai/state_machine/fsm.py
    tests/test_fsm.py
  </files>
  <read_first>
    @src/loopai/state_machine/fsm.py (_handle_act 方法 — 特别是 Step 3 和 Step 5 的 tool_result 注入)
    @src/loopai/tools/types.py (ToolResult — overflow_file 字段)
  </read_first>
  <action>
    修改 `src/loopai/state_machine/fsm.py` 的 `_handle_act()`：

    1. 在现有的 Step 5（注入 tool_result 到 session messages）处修改：
       当前代码（~363-370 行）：
       ```python
       tool_content = (
           str(result.data) if result.data is not None
           else (result.error_message or "")
       )
       session.add_message("tool", content=tool_content, tool_call_id=tool_call_id)
       ```
       修改为：
       ```python
       # Check if result was written to overflow file (Phase 3, D-05)
       if result.overflow_file and result.data is not None:
           # Calculate size for the reference
           data_str = str(result.data)
           size_kb = len(data_str.encode("utf-8")) // 1024
           tool_content = (
               f"[工具输出已保存至: {result.overflow_file} ({size_kb}KB)]\n"
               f"如需查看完整内容，请使用 Bash 工具读取该文件。\n"
               f"--- 预览 (前 500 字符) ---\n"
               f"{data_str[:500]}"
           )
       else:
           tool_content = (
               str(result.data) if result.data is not None
               else (result.error_message or "")
           )
       session.add_message("tool", content=tool_content, tool_call_id=tool_call_id)
       ```
       参考格式来自 D-05: `[工具输出已保存至: .sandbox/overflow/xxx.txt (123KB)]`
       前置 500 字符预览确保 LLM 可以快速判断是否需要读取完整文件。

       D-06: Agent 可用 Bash 工具后续读取溢出文件内容。所以在引用中提示"使用 Bash 工具读取"。

    2. 在注入 tool_content 之后发布 overflow_written 事件（发布到 EventBus）：
       在 session.add_message 之后：
       ```python
       # Publish overflow_written event
       if result.overflow_file:
           await self.bus.publish(
               "overflow_written",
               {
                   "event_type": "overflow_written",
                   "session_id": session.session_id,
                   "step_num": step_num,
                   "tool_name": tool_name,
                   "tool_call_id": tool_call_id,
                   "file_path": result.overflow_file,
                   "size_kb": size_kb,
               },
           )
       ```

    3. 追加式固化验证：不需要额外实现代码。`session.add_message()` 已经是 append-only。
       验证方式：在 test_fsm.py 中为压缩场景添加断言：
       - 压缩后 session.messages 长度为 (1 summary + N preserved messages)
       - 压缩前 session.messages 的后 N 条消息与压缩后的后 N 条完全一致

    修改 `tests/test_fsm.py`：

    4. 在 `TestContextManagement` 类中（或新建测试类）添加：
       - `test_overflow_file_reference_in_content`：
         - mock executor.execute() 返回 ToolResult.success(data="x" * 90000, overflow_file=".sandbox/overflow/test.txt")
         - 验证 tool_content 包含 `[工具输出已保存至:` 和文件路径
         - 验证 tool_content 包含前 500 字符预览
       - `test_no_overflow_reference_normal_output`：
         - mock executor.execute() 返回 ToolResult.success(data="normal output")
         - 验证 tool_content 为 "normal output"（不含溢出引用格式）
       - `test_append_only_after_compression`：
         - 压缩场景验证：压缩前的最后 N 轮消息在压缩后依然存在且内容一致
       - `test_overflow_written_event_published`：
         - 验证当 result.overflow_file 非空时，overflow_written 事件被发布
  </action>
  <verify>
    <automated>cd /home/atis/HE/loopAI && python -m pytest tests/test_fsm.py -x -v -k "overflow or append_only" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 工具结果有 overflow_file 时，tool_content 包含 "[工具输出已保存至:" 格式引用
    - 引用包含文件路径、文件大小、前 500 字符预览、和"使用 Bash 工具读取"提示
    - 工具结果无 overflow_file 时，tool_content 与之前行为一致（直接注入 data）
    - overflow_written 事件在溢出文件写入时发布
    - 压缩操作保持了最后 N 轮消息的完整性和顺序（追加式原则验证）
  </acceptance_criteria>
  <done>
    _handle_act 正确使用溢出文件引用替换上下文内的完整内容，追加式原则验证通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSM -> TokenGuard -> ContextCompressor | 消息列表在守卫和压缩器之间传递 |
| FSM._handle_act -> overflow file reference | 工具输出引用字符串注入上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T (Tampering) | session.messages after compression | mitigate | 压缩仅通过 session.messages.clear() + extend() 替换消息列表。原始消息已持久化在 JSONL 中，可通过日志恢复 |
| T-03-03-02 | D (DoS) | summary LLM call | accept | 摘要调用会增加一次 LLM 往返（产生额外 token 消耗和延迟），但这是必要的压缩成本。后续可优化为使用更小的模型 |
| T-03-03-03 | S (Spoofing) | overflow file reference | mitigate | 引用字符串中使用文件路径（基于 session_id + tool_call_id 生成），不允许注入用户可控内容 |
</threat_model>

<verification>
全量回归：`cd /home/atis/HE/loopAI && python -m pytest tests/ -x -v`
</verification>

<success_criteria>
- FSM._handle_reason 在 LLM 调用前集成 TokenGuard 和 ContextCompressor
- FSM._handle_act 在工具输出 >80K 时使用溢出文件引用替换完整内容
- 所有消息操作遵循追加式原则
- 全部测试通过（包括现有 196+ 回归测试）
- 无 file_modified 冲突的遗留代码
</success_criteria>

<output>
完成后创建 `.planning/phases/03-context-management/03-03-SUMMARY.md`
</output>
