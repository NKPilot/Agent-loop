---
phase: 05-observability
plan: 06
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-05
files_modified:
  - frontend/src/components/ToolDetail.tsx
  - frontend/src/components/TokenUsageCard.tsx
  - frontend/src/components/ConfirmationDialog.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - OBS-03
  - OBS-04
  - OBS-05

must_haves:
  truths:
    - "右侧面板展示选中工具调用的参数（JSON 语法高亮）、结果、执行耗时和状态标记"
    - "Token 使用卡片实时显示 prompt tokens、completion tokens、session 总数和预估成本"
    - "recharts 图表展示会话 token 消耗随时间变化（AreaChart 累积成本曲线）"
    - "危险命令确认弹窗显示命令详情（工具名、完整命令、标记原因），批准/拒绝按钮可用"
    - "用户可以在会话历史中切换三个 Tab（Timeline / Token Usage / Raw Events）查看不同视图"
  artifacts:
    - path: "frontend/src/components/ToolDetail.tsx"
      provides: "工具调用详情面板：参数 JSON 展示、结果内容、元数据（ID、耗时、token）"
    - path: "frontend/src/components/TokenUsageCard.tsx"
      provides: "Token/成本摘要卡片 + recharts 图表（PieChart、AreaChart、BarChart）"
    - path: "frontend/src/components/ConfirmationDialog.tsx"
      provides: "危险命令确认弹窗：命令详情、批准/拒绝操作、超时倒计时"
  key_links:
    - from: "ToolDetail.tsx"
      to: "useUIStore(s => s.selectedToolCallId)"
      via: "Zustand selector"
      pattern: "useUIStore"
    - from: "ConfirmationDialog.tsx"
      to: "api.ts confirmCommand()"
      via: "fetch POST /api/sessions/{id}/confirm"
      pattern: "confirmCommand"
    - from: "TokenUsageCard.tsx"
      to: "costCalculator.ts calculateCost()"
      via: "import"
      pattern: "calculateCost"
---

<objective>
构建右侧面板三件套：ToolDetail（工具调用详情）、TokenUsageCard（Token/成本追踪 + recharts 图表）、ConfirmationDialog（危险命令确认弹窗）。同时实现会话历史浏览的多 Tab 视图（Timeline / Token Usage / Raw Events）。覆盖 OBS-03（工具详情）、OBS-04（Token/成本追踪）、OBS-05（会话历史浏览）和 D-06（危险操作确认）。

Purpose: 这是最后一批前端面板组件，完成 D-05 全功能交付目标。用户可以通过右侧面板深入了解工具调用细节，实时追踪 token 消耗和成本，并在网页中批准/拒绝危险操作。
Output: 完整的右侧面板（ToolDetail + TokenUsageCard）、危险命令确认弹窗、会话历史多 Tab 视图。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-observability/05-UI-SPEC.md
@.planning/phases/05-observability/05-CONTEXT.md
@frontend/src/lib/eventTypes.ts
@frontend/src/lib/api.ts
@frontend/src/lib/costCalculator.ts
@frontend/src/stores/uiStore.ts
@frontend/src/stores/eventStore.ts
@frontend/src/components/AgentTimeline.tsx
@frontend/src/App.tsx

<interfaces>
From frontend/src/lib/eventTypes.ts:
```typescript
// ToolDetail needs:
interface ToolCallInfo { tool_name, tool_call_id, step_num, full_args?, result?, is_error?, duration_ms?, status }
interface TokenUsage { prompt_tokens, completion_tokens, total_tokens }
interface ConfirmationRequiredEvent { event_type: "confirmation_required", confirmation_id, tool_name, tool_args, permission_level, reason, step_num }

// TokenUsageCard needs:
interface StepEndEvent { token_usage?: TokenUsage }
interface TokenWarningEvent { token_count, max_tokens, used_pct, action }
```

From frontend/src/lib/api.ts:
```typescript
function confirmCommand(sessionId: string, confirmationId: string, approved: boolean): Promise<void>
function fetchSessions(): Promise<SessionSummary[]>
function fetchSession(id: string): Promise<SessionDetail>
```

From frontend/src/lib/costCalculator.ts:
```typescript
function calculateCost(promptTokens: number, completionTokens: number, rates?: CostRates): number
function formatCost(cost: number): string  // "$0.042"
function formatTokens(count: number): string  // "1,234"
interface CostRates { promptPer1K: number; completionPer1K: number }
```

From frontend/src/stores/uiStore.ts:
```typescript
// ToolDetail reads:
selectedToolCallId: string | null
selectToolCall: (id: string | null) => void

// ConfirmationDialog reads/writes:
pendingConfirmation: ConfirmationRequiredEvent | null
setPendingConfirmation: (event: ConfirmationRequiredEvent | null) => void
```

From frontend/src/stores/eventStore.ts:
```typescript
toolCallsBySession: Record<string, ToolCallInfo[]>
eventsBySession: Record<string, Event[]>
getSessionToolCalls: (sessionId: string) => ToolCallInfo[]
getSessionEvents: (sessionId: string) => Event[]
```

From UI-SPEC.md:
- Tool Detail section (第 201-210 行): Header (tool name + status badge + duration), Arguments (JSON syntax-highlighted monospace), Result (text or truncated + overflow reference), Metadata (tool_call_id, token, cost)
- Token/Cost Display Contract (第 296-321 行): TokenUsageCard format, TokenWarning display, Cost Tracking Chart (AreaChart)
- Confirmation Dialog (第 239-248 行): "Dangerous Command Detected" title, AlertTriangle icon destructive color, tool name + full command (monospace scrollable), "Approve Command" destructive button, "Reject Command" outline button
- Session history tabs: Timeline / Token Usage / Raw Events (Tabs component per UI-SPEC Design System)
- Copywriting: exact strings for dialog title, approve/reject buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolDetail 组件（右侧面板）</name>
  <files>frontend/src/components/ToolDetail.tsx, frontend/src/App.tsx</files>
  <read_first>
    - .planning/phases/05-observability/05-UI-SPEC.md — Tool Detail section（第 201-210 行）：Header（工具名+状态 Badge+耗时）、Arguments（JSON 高亮 monospace）、Result（文本或截断+溢出文件引用）、Metadata（tool_call_id、token、cost）
    - .planning/phases/05-observability/05-UI-SPEC.md — Badge 颜色映射：done=success 绿色、error=destructive 红色、running=default、pending=muted
    - frontend/src/lib/eventTypes.ts — ToolCallInfo 接口（tool_name, tool_call_id, step_num, full_args, result, is_error, duration_ms, status）
    - frontend/src/stores/uiStore.ts — selectedToolCallId, selectToolCall
    - frontend/src/stores/eventStore.ts — toolCallsBySession, getSessionToolCalls
    - frontend/src/App.tsx — 三面板布局（需将右面板空壳替换为 ToolDetail）
  </read_first>
  <action>
    1. 创建 `frontend/src/components/ToolDetail.tsx`：
       - 从 `useUIStore(s => s.selectedToolCallId)` 读取选中的工具调用 ID
       - 从 `useUIStore(s => s.activeSessionId)` 读取活跃会话 ID
       - 从 `useEventStore(s => s.toolCallsBySession[activeSessionId])` 获取工具调用列表，find 匹配的 ToolCallInfo
       - 如果无选中工具调用：渲染 "Select a tool call" 空状态（标题 "Select a tool call"，副标题 "Click any tool call in the timeline to view its arguments, result, and performance metrics." —— per UI-SPEC copywriting 精确文案）
       - 如果有选中工具调用：
         - **Header**：工具名称（Heading 20px）+ status Badge（per UI-SPEC badge mapping：done=success、error=destructive、running=default）+ 耗时（`{duration_ms}ms`，Label 12px）
         - **Arguments**：使用 `<pre>` 标签 + monospace 字体（`font-mono`）渲染 `JSON.stringify(full_args, null, 2)`。使用 CSS 对 JSON 内容做简单的语法着色（字符串值用绿色、数字用蓝色、布尔用琥珀色）——通过正则替换或 `<span>` 包裹实现
         - **Result**：渲染 `result` 文本。如果包含 `[工具输出已保存至:` 引用（溢出文件格式），显示截断预览 + 文件路径链接。使用 `<pre>` + monospace。如果 `is_error` 为 true，添加红色左边框（`border-l-2 border-destructive`）和 Error Badge
         - **Metadata**：tool_call_id（截断显示，Label 12px + `text-muted-foreground`）、token 消耗（如果有，从 StepEnd.token_usage 计算）、预估成本
         - 使用 shadcn/ui Card 组件包裹每个部分（CardHeader + CardContent），Separator 分隔各部分
         - 使用 ScrollArea 包裹整个面板（per UI-SPEC 360px 右侧面板可滚动）
       - 内容过渡使用 CSS `transition: opacity 150ms ease`（per UI-SPEC Streaming Content Rendering）
    2. 更新 `frontend/src/App.tsx`：将右侧面板中的空状态占位替换为 `<ToolDetail />` 组件导入和渲染。
    3. 验证：`pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build`。
  </action>
  <acceptance_criteria>
    - `frontend/src/components/ToolDetail.tsx` 存在
    - `grep -c "selectedToolCallId" frontend/src/components/ToolDetail.tsx` 输出 >= 1
    - `grep -c "Select a tool call" frontend/src/components/ToolDetail.tsx` 输出 >= 1
    - `grep -c "JSON.stringify\|full_args" frontend/src/components/ToolDetail.tsx` 输出 >= 1
    - `grep -c "font-mono" frontend/src/components/ToolDetail.tsx` 输出 >= 2（Arguments + Result 两处 monospace）
    - `grep -c "duration_ms\|is_error" frontend/src/components/ToolDetail.tsx` 输出 >= 2
    - `grep -c "ToolDetail" frontend/src/App.tsx` 输出 >= 1
    - `pnpm --dir frontend tsc --noEmit` 退出码为 0
    - `pnpm --dir frontend build` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build</automated>
  </verify>
  <done>ToolDetail 组件展示选中工具调用的完整信息：参数 JSON、结果内容、状态标记、耗时和元数据。空状态文案匹配 UI-SPEC。</done>
</task>

<task type="auto">
  <name>Task 2: TokenUsageCard + recharts 图表 + 会话历史 Tabs（OBS-04, OBS-05）</name>
  <files>frontend/src/components/TokenUsageCard.tsx, frontend/src/App.tsx</files>
  <read_first>
    - .planning/phases/05-observability/05-UI-SPEC.md — Token/Cost Display Contract（第 296-321 行）：TokenUsageCard metrics（prompt tokens 逗号分隔、completion tokens、session total、estimated cost $0.042、context window % + progress bar）、Token Warning Display（琥珀 Badge + progress bar）、Cost Tracking Chart（AreaChart 累积成本 vs 步骤数）
    - .planning/phases/05-observability/05-UI-SPEC.md — recharts Chart Types 表：PieChart（token 拆分）、LineChart（token 随时间消耗）、BarChart（工具调用耗时）、AreaChart（累积 token）
    - .planning/phases/05-observability/05-UI-SPEC.md — Tabs 组件："Timeline"、"Token Usage"、"Raw Events" 三个 tab
    - frontend/src/lib/eventTypes.ts — StepEndEvent.token_usage, TokenUsage 接口
    - frontend/src/lib/costCalculator.ts — calculateCost, formatCost, formatTokens
    - frontend/src/stores/eventStore.ts — eventsBySession, getSessionEvents
    - frontend/src/stores/uiStore.ts — activeSessionId
    - .planning/phases/05-observability/05-CONTEXT.md — Claude's Discretion re: recharts 图表类型选择
    - .planning/phases/05-observability/05-UI-SPEC.md — Tab 名称："Timeline"、"Token Usage"、"Raw Events"（not "t"、"u"、"r"）
  </read_first>
  <action>
    1. 创建 `frontend/src/components/TokenUsageCard.tsx`：
       - 从 `useEventStore(s => s.eventsBySession[activeSessionId])` 获取当前会话所有事件
       - 计算累积指标（从所有 StepEnd 事件中提取 token_usage）：
         - `totalPromptTokens` = sum of all StepEnd.token_usage.prompt_tokens
         - `totalCompletionTokens` = sum of all StepEnd.token_usage.completion_tokens
         - `totalTokens` = totalPromptTokens + totalCompletionTokens
         - `estimatedCost` = calculateCost(totalPromptTokens, totalCompletionTokens)
         - `contextWindowUsage`：从最近一次 TokenWarning 事件或最后一次 StepEnd.total_tokens / 128000 计算百分比
       - **Summary Card**（shadcn/ui Card）：
         - 网格布局显示 4 个指标：Prompt Tokens、Completion Tokens、Total Tokens、Estimated Cost
         - 每个指标使用 Label (12px) 标签 + Body (14px) 数值，数值用 `formatTokens()`/`formatCost()` 格式化
         - Cost 指标突出显示（使用 primary 颜色或更大字号）
       - **Context Window Progress Bar**（shadcn/ui Progress 组件）：
         - 显示 "Context Window: 45%" 等，Progress 值 = used_pct
         - 颜色根据阈值变化：< 75% = default（primary）、75-90% = warning（amber）、> 90% = destructive（red）
         - 如果有 TokenWarning 事件，在 Progress 旁边显示 amber Badge "Token {used_pct}%"
       - **recharts AreaChart** — Cumulative Cost vs Steps：
         - X 轴：步骤编号（step_num）
         - Y 轴：累积成本（USD）
         - 数据点：遍历事件数组，在每个 StepEnd 处累加 token_usage，计算累积成本
         - 响应式容器（`<ResponsiveContainer width="100%" height={200}>`）
         - 图表着色使用 CSS 变量 `hsl(var(--primary))`（通过 recharts 的 stroke/fill 属性）
       - **recharts PieChart** — Token Breakdown：
         - 两个扇区：Prompt Tokens（蓝色/primary）和 Completion Tokens（绿色/success）
         - 图例显示数量和百分比
         - 高度约 180px
       - 如果会话没有 token 数据（无 StepEnd 事件），显示 "No token data available yet" 提示
    2. 更新 `frontend/src/App.tsx`：
       - 在中间面板（AgentTimeline 所在区域）添加 shadcn/ui Tabs 组件，包含三个 Tab：
         - "Timeline"（默认活动）-> 渲染 AgentTimeline 组件
         - "Token Usage" -> 渲染 TokenUsageCard 组件（包含 Summary Card + Progress + 图表）
         - "Raw Events" -> 渲染原始事件 JSON 列表（使用 `<pre>` + `font-mono` + `text-xs`，每行一个 JSON 对象，使用 `JSON.stringify(event, null, 2)` + ScrollArea 包裹）
       - Tab 切换使用 useState 或 URL hash
       - Tab 名称使用 UI-SPEC 规定的精确文案："Timeline"、"Token Usage"、"Raw Events"
    3. 键盘导航：`t` 切换到 Timeline、`u` 切换到 Token Usage、`r` 切换到 Raw Events（在 App.tsx 的 keydown handler 中添加）
    4. 验证：`pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build`。确认 recharts 图表组件被正确导入（`import { AreaChart, Area, PieChart, Pie, Cell, ResponsiveContainer, XAxis, YAxis, Tooltip as RechartsTooltip, Legend } from 'recharts'`）。注意 Tooltip 与 shadcn/ui Tooltip 命名冲突——使用 import alias。
  </action>
  <acceptance_criteria>
    - `frontend/src/components/TokenUsageCard.tsx` 存在
    - `grep -c "AreaChart\|PieChart\|ResponsiveContainer" frontend/src/components/TokenUsageCard.tsx` 输出 >= 3
    - `grep -c "calculateCost\|formatCost\|formatTokens" frontend/src/components/TokenUsageCard.tsx` 输出 >= 3
    - `grep -c "totalPromptTokens\|totalCompletionTokens" frontend/src/components/TokenUsageCard.tsx` 输出 >= 2
    - `grep -c "Progress" frontend/src/components/TokenUsageCard.tsx` 输出 >= 1
    - `grep -c "Tabs\|TabsList\|TabsTrigger\|TabsContent" frontend/src/App.tsx` 输出 >= 4（shadcn/ui Tabs 组件）
    - `grep -c "Timeline.*Token Usage.*Raw Events" frontend/src/App.tsx` 输出 >= 1
    - `grep -c "key.*===.*'t'\|key.*===.*'u'\|key.*===.*'r'" frontend/src/App.tsx` 输出 >= 3（键盘导航 Tab 切换）
    - `pnpm --dir frontend tsc --noEmit` 退出码为 0
    - `pnpm --dir frontend build` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build</automated>
  </verify>
  <done>Token/成本追踪面板就绪：Summary Card + Context Window Progress + AreaChart（累积成本）+ PieChart（token 拆分）。会话历史支持三 Tab 视图（Timeline / Token Usage / Raw Events）。</done>
</task>

<task type="auto">
  <name>Task 3: ConfirmationDialog 危险命令确认弹窗（D-06）</name>
  <files>frontend/src/components/ConfirmationDialog.tsx, frontend/src/App.tsx, frontend/src/hooks/useSessionEvents.ts</files>
  <read_first>
    - .planning/phases/05-observability/05-UI-SPEC.md — Confirmation Dialog section（第 239-248 行）：Dialog title "Dangerous Command Detected"、AlertTriangle icon destructive color、Body 显示工具名+完整命令（monospace scrollable）+标记原因、"Approve Command" destructive button、"Reject Command" outline button、"Dangerous Command" 标题（per Copywriting Contract 第 287 行精确文案）
    - .planning/phases/05-observability/05-RESEARCH.md — Pitfall 5 (Confirmation Timeout Mismatch)：包含 timeout_seconds 在事件中，显示倒计时、超时自动关闭
    - frontend/src/stores/uiStore.ts — pendingConfirmation, setPendingConfirmation, activeSessionId
    - frontend/src/lib/api.ts — confirmCommand(sessionId, confirmationId, approved)
    - frontend/src/lib/eventTypes.ts — ConfirmationRequiredEvent 接口
    - frontend/src/hooks/useSessionEvents.ts — SSE 事件处理中 confirmation_required 事件的分发
  </read_first>
  <action>
    1. 创建 `frontend/src/components/ConfirmationDialog.tsx`：
       - 使用 shadcn/ui Dialog 组件（controlled open state）
       - 从 `useUIStore(s => s.pendingConfirmation)` 读取待确认事件
       - Dialog 在 `pendingConfirmation !== null` 时自动打开，关闭时调用 `setPendingConfirmation(null)`
       - **Dialog 内容**：
         - 标题："Dangerous Command"（per UI-SPEC Copywriting Contract 第 287 行精确文案）
         - Icon：`AlertTriangle`（lucide-react）在标题旁，使用 `text-destructive` 颜色（red）
         - Body 第一段：解释文字 "The agent wants to execute a potentially dangerous command. Review the command below and approve or reject it."
         - **工具名**：Label 12px + 工具名称 Body 14px 粗体
         - **命令详情**：`<pre>` 标签 + `font-mono` + `text-sm` + ScrollArea 包裹，显示 `JSON.stringify(tool_args, null, 2)` 或直接显示关键参数（取决于 tool_args 结构）
         - **标记原因**：`reason` 字段内容（如 "命中黑名单命令 rm"），使用 amber/warning 颜色 Alert 组件显示
         - **Permission Level**：Badge 显示 "dangerous"（destructive variant）
         - **超时倒计时**：显示 "120 秒后自动拒绝" 或倒计时数字（per Pitfall 5）。使用 `confirmation_timeout`（从 config 推算或在事件中包含 timeout_seconds 字段）。如果事件不包含 timeout_seconds，使用默认 120 秒。用 `useEffect` + `setInterval` 实现倒计时
         - **操作按钮**：
           - "Approve Command"（shadcn/ui Button，variant="destructive"）-> 调用 `confirmCommand(sessionId, confirmationId, true)`，成功后调用 `setPendingConfirmation(null)` 关闭 Dialog
           - "Reject Command"（shadcn/ui Button，variant="outline"）-> 调用 `confirmCommand(sessionId, confirmationId, false)`，成功后关闭
         - Dialog 不可通过点击背景关闭（`DialogContent` 的 `onInteractOutside` 阻止）——用户必须明确选择批准或拒绝
         - Escape 键关闭 -> 等同于拒绝（调用 confirmCommand false + 关闭）
       - 批准/拒绝期间显示 loading 状态（`Button` 的 `disabled` + spinner）
       - 如果 API 调用失败，显示错误提示（Toast 或 Alert inline）
    2. 更新 `frontend/src/hooks/useSessionEvents.ts`（Plan 04 创建）：确保 `confirmation_required` 事件到达时调用 `useUIStore.getState().setPendingConfirmation(data)`——将事件数据存入 Zustand store，触发 ConfirmationDialog 打开。
    3. 更新 `frontend/src/App.tsx`：在组件树的顶层（Dialog 是 portal 渲染的）渲染 `<ConfirmationDialog />`。由于 shadcn/ui 的 Dialog 使用 Radix Portal（渲染到 document.body），只需将组件放在 App 的 JSX 中即可。
    4. 验证：`pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build`。
  </action>
  <acceptance_criteria>
    - `frontend/src/components/ConfirmationDialog.tsx` 存在
    - `grep -c "pendingConfirmation" frontend/src/components/ConfirmationDialog.tsx` 输出 >= 1
    - `grep -c "confirmCommand" frontend/src/components/ConfirmationDialog.tsx` 输出 >= 2（Approve + Reject 两处调用）
    - `grep -c "AlertTriangle" frontend/src/components/ConfirmationDialog.tsx` 输出 >= 1
    - `grep -c "destructive" frontend/src/components/ConfirmationDialog.tsx` 输出 >= 2（Approve button + icon color）
    - `grep -c "Approve Command\|Reject Command" frontend/src/components/ConfirmationDialog.tsx` 输出 >= 2（精确文案）
    - `grep -c "setPendingConfirmation.*confirmation_required\|confirmation_required.*setPendingConfirmation" frontend/src/hooks/useSessionEvents.ts` 输出 >= 1
    - `grep -c "ConfirmationDialog" frontend/src/App.tsx` 输出 >= 1
    - `pnpm --dir frontend tsc --noEmit` 退出码为 0
    - `pnpm --dir frontend build` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend tsc --noEmit && pnpm --dir frontend build</automated>
  </verify>
  <done>ConfirmationDialog 弹窗在危险命令检测到时自动弹出：显示命令详情、批准/拒绝按钮、倒计时超时。用户操作通过 REST API 发送到后端 PermissionGuard。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->POST /api/sessions/{id}/confirm | 用户批准/拒绝危险命令 |
| Tool result->DOM | 工具执行结果（可能包含用户文件内容）渲染为 HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Information Disclosure | ToolDetail.tsx | mitigate | 工具参数和结果使用 React 默认文本转义，不通过 dangerouslySetInnerHTML 渲染。JSON 语法着色通过 CSS 类实现，不使用 innerHTML。 |
| T-05-18 | Tampering | ConfirmationDialog.tsx | mitigate | 确认请求通过 POST /api/sessions/{id}/confirm 发送——后端验证 confirmation_id 存在后才调用 PermissionGuard.respond()。伪造的 confirmation_id 返回 404。 |
| T-05-19 | Repudiation | ConfirmationDialog.tsx | accept | 用户确认/拒绝操作记录在 JSONL 日志中（ConfirmationResponse 事件）。审计轨迹完整。无额外防抵赖需求。 |
| T-05-20 | Information Disclosure | Raw Events Tab (App.tsx) | accept | "Raw Events" Tab 显示完整事件 JSON——包含 session_id、工具参数和结果。v1 本地单用户工具，无多用户隔离需求。 |
</threat_model>

<verification>
运行 `pnpm --dir frontend tsc --noEmit` 验证类型安全。
运行 `pnpm --dir frontend build` 验证构建成功。
手动验证：启动后端 + 前端，触发工具调用确认流程（需要 agent 调用危险命令）。
</verification>

<success_criteria>
- ToolDetail 正确展示选中工具调用的参数、结果、状态和元数据
- TokenUsageCard 实时显示 token 消耗和预估成本
- recharts AreaChart 展示累积成本曲线，PieChart 展示 token 拆分
- 会话历史可通过三个 Tab（Timeline / Token Usage / Raw Events）切换浏览
- ConfirmationDialog 在危险命令时弹出，批准/拒绝通过 API 发送到后端
- `pnpm --dir frontend build` 成功
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability/05-06-SUMMARY.md`
</output>
