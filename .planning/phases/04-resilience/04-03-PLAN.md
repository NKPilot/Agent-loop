---
phase: 04-resilience
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/loopai/tools/types.py
  - src/loopai/tools/executor.py
  - src/loopai/tools/registry.py
  - src/loopai/state_machine/fsm.py
  - src/loopai/main.py
  - tests/test_tools.py
  - tests/test_fsm.py
autonomous: true
requirements: [RES-04, RES-05, RES-06]
user_setup: []

must_haves:
  truths:
    - "ToolExecutor 错误恢复升级为 4 层：外观修复 → 上下文内重试 → 退避重试 → 人工升级"
    - "外观修复在 JSON 解析错误时自动修正参数格式后重试"
    - "达到人工升级层时发布 escalation_needed 事件"
    - "GuardPipeline 在 _handle_reason 中顺序执行 TokenGuard → CostGuard → RateLimitGuard"
    - "CircuitBreaker 在 _handle_act 中阻止 OPEN 状态的工具执行"
    - "CircuitBreaker 的工具执行结果记录后触发状态变更"
    - "CheckpointManager 在每个状态转换后保存会话状态"
    - "FailureRegistry 在 _handle_act 中检查并记录失败操作"
    - "CircuitBreaker OPEN 状态的工具从 LLM tool schema 中过滤排除"
    - "RateLimitGuard.record_call() 在工具执行后被调用"
    - "main.py run_session() 创建并注入所有 Phase 4 组件"
  artifacts:
    - path: "src/loopai/tools/types.py"
      provides: "RecoveryLayer enum, RecoveryConfig dataclass"
      min_lines: 20
      contains: "class RecoveryLayer"
    - path: "src/loopai/tools/executor.py"
      provides: "_execute_with_recovery 4-layer retry loop"
      min_lines: 120
      contains: "_execute_with_recovery"
    - path: "src/loopai/tools/registry.py"
      provides: "get_schemas() with exclude_open filtering"
      min_lines: 30
      contains: "exclude_open"
    - path: "src/loopai/state_machine/fsm.py"
      provides: "All Phase 4 components wired into FSM"
      min_lines: 200
      contains: "guard_pipeline"
  key_links:
    - from: "fsm._handle_reason"
      to: "GuardPipeline"
      via: "guard_pipeline.check(messages)"
      pattern: "guard_pipeline"
    - from: "fsm._handle_act"
      to: "CircuitBreaker"
      via: "circuit_breaker.check(tool_name)"
      pattern: "circuit_breaker"
    - from: "fsm.run()"
      to: "CheckpointManager"
      via: "checkpoint_manager.save(session)"
      pattern: "checkpoint_manager"
    - from: "fsm._handle_act"
      to: "FailureRegistry"
      via: "failure_registry.should_skip() / .record()"
      pattern: "failure_registry"
    - from: "fsm._handle_act"
      to: "RateLimitGuard"
      via: "rate_limit_guard.record_call(tool_name)"
      pattern: "record_call"
---

<objective>
将 Phase 4 所有韧性与恢复组件集成到现有主循环中：4 层恢复执行管线、Registry 熔断过滤、FSM 全组件接线、main.py 组件创建与注入。

Purpose: ToolExecutor 升级为 4 层恢复（RES-05），ToolRegistry 支持熔断器过滤（RES-06 要求），FSM 集成 GuardPipeline（RES-04）、CheckpointManager（RES-01）、CircuitBreaker（RES-06）、FailureRegistry（RES-03），更新 main.py 创建并注入所有组件。

Output:
- RecoveryConfig + RecoveryLayer 类型定义
- ToolExecutor._execute_with_recovery 4 层恢复管线
- ToolRegistry.get_schemas(exclude_open) 过滤
- ReActFSM 全组件接线：6 个新参数 + 5 个集成点
- main.py run_session() 创建并注入所有 Phase 4 组件
- 集成测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-resilience/04-CONTEXT.md
@.planning/phases/04-resilience/04-RESEARCH.md

<interfaces>
<!-- All interfaces from Plan 01 and Plan 02 that this plan consumes -->

From src/loopai/resilience/checkpoint.py:
```python
class CheckpointManager:
    def __init__(self, session_id: str, log_dir: str = "logs/sessions") -> None: ...
    def save(self, session: Session) -> dict: ...
    @classmethod
    def recover(cls, session_id: str, log_dir: str = "logs/sessions") -> Session | None: ...
    async def close(self) -> None: ...
```

From src/loopai/resilience/failure_registry.py:
```python
class FailureRegistry:
    def __init__(self, session_id: str, log_dir: str = "logs/sessions") -> None: ...
    def record(self, tool_name: str, signature: str, error_message: str) -> None: ...
    def should_skip(self, tool_name: str, signature: str) -> bool: ...
    def list_failures(self, tool_name: str) -> list[str]: ...
```

From src/loopai/resilience/circuit_breaker.py:
```python
class CircuitState(StrEnum):
    CLOSED = "closed"
    OPEN = "open"
    HALF_OPEN = "half_open"

class CircuitBreaker:
    def __init__(self, window_size: int = 10, failure_threshold: float = 0.5, cooldown_seconds: float = 30.0) -> None: ...
    def check(self, tool_name: str) -> tuple[bool, CircuitState]: ...
    async def record(self, tool_name: str, success: bool, bus: EventBus | None = None) -> None: ...
    async def record_with_session(self, tool_name: str, success: bool, session_id: str, bus: EventBus | None = None) -> None: ...
    def get_open_tools(self) -> set[str]: ...
    def get_tool_state(self, tool_name: str) -> CircuitState: ...
    def reset_tool(self, tool_name: str) -> None: ...
```

From src/loopai/state_machine/guards.py (extended in Plan 02):
```python
class GuardResult:
    action: str        # "ok", "compress", "blocked", "warn"
    guard_name: str | None
    detail: str | None

class GuardPipeline:
    def __init__(self, guards: list) -> None: ...
    def check(self, messages: list[dict]) -> GuardResult: ...

class CostGuard:
    def __init__(self, max_cost_per_call: float = 0.05, ...) -> None: ...
    def check(self, messages: list[dict], token_count: int | None = None, model_name: str = "gpt-4o") -> GuardResult: ...

class RateLimitGuard:
    def __init__(self, max_calls: int = 10, window_seconds: float = 60.0) -> None: ...
    def check(self, tool_name: str) -> GuardResult: ...
    def record_call(self, tool_name: str) -> None: ...

class LoopClassification(StrEnum):
    LOOP_EXACT_SAME = "exact_same"
    LOOP_SAME_TOOL = "same_tool"
    LOOP_STUCK = "stuck"

# Updated LoopDetector.check() signature:
# def check(self, tool_name, arguments) -> tuple[bool, str, LoopClassification | None]:
```

Current FSM constructor signature:
```python
class ReActFSM:
    def __init__(
        self, client, bus, budget_guard, loop_detector, message_validator,
        registry, executor, permission_guard,
        *, token_guard=None, compressor=None,
    ) -> None: ...
```

Current FSM.run() loop (96-106):
```python
while session.state not in (AgentState.FINISH, AgentState.ERROR):
    if session.state == AgentState.REASON:
        await self._handle_reason(session)
    elif session.state == AgentState.ACT:
        await self._handle_act(session)
    elif session.state == AgentState.OBSERVE:
        await self._handle_observe(session)
    else:
        session.state = AgentState.ERROR
```

Current FSM._handle_reason (123-272):
- Line 148: MessageValidator.validate()
- Line 151-186: TokenGuard check + compression (Phase 3)
- Line 189: BudgetGuard.check()
- Line 198: LLM call

Current FSM._handle_act (274-450):
- Line 309: LoopDetector.check()
- Line 346: Registry lookup
- Line 357: PermissionGuard.check()
- Line 389: executor.execute()

Current main.py run_session():
```python
async def run_session(prompt, config, max_steps_override=None):
    bus = EventBus()
    session = Session(config=config)
    # ... messages setup ...
    client = LLMClient(config, bus)
    budget_guard = BudgetGuard(max_steps=actual_max)
    loop_detector = LoopDetector()
    message_validator = MessageValidator()
    registry = ToolRegistry()
    executor = ToolExecutor(registry)
    permission_guard = PermissionGuard(bus, ...)
    # Bash tool registration
    # Phase 3 components
    token_counter = TokenCounter()
    token_guard = TokenGuard(...)
    compressor = ContextCompressor(...)
    fsm = ReActFSM(client, bus, ..., token_guard=token_guard, compressor=compressor)
    # Start consumers
    # Run FSM
    # Shutdown
```

From src/loopai/tools/registry.py:
```python
class ToolRegistry:
    def get_schemas(self) -> list[dict]:
        """Return OpenAI function-calling JSON Schemas for all registered tools."""
        return [meta.to_openai_schema() for meta in self._tools.values()]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecoveryConfig + ToolExecutor 4 层恢复 + Registry 过滤</name>
  <files>
    src/loopai/tools/types.py
    src/loopai/tools/executor.py
    src/loopai/tools/registry.py
    tests/test_tools.py
  </files>
  <read_first>
    src/loopai/tools/types.py — 阅读 RetryConfig 模式和类定义风格（第 63-95 行）
    src/loopai/tools/executor.py — 阅读现有 _execute_with_retry（第 117-176 行）方法完整逻辑
    src/loopai/tools/registry.py — 阅读 get_schemas() 当前实现（第 107-124 行）
  </read_first>
  <action>
    **步骤 A — 在 tools/types.py 中添加 RecoveryLayer 和 RecoveryConfig**

    在 ToolMetadata 类定义之前、PermissionLevel 枚举之后追加：

    ```python
    class RecoveryLayer(IntEnum):
        """Recovery escalation layers (D-03, D-04).
    
        Each layer has independent entry conditions.
        """
        COSMETIC = 1       # Fix argument formatting, retry
        IN_CONTEXT = 2     # LLM sees structured error, self-corrects (FSM level)
        BACKOFF = 3        # Exponential backoff retry (existing)
        ESCALATE = 4       # Pause, escalate to human
    
    
    class RecoveryConfig(BaseModel):
        """Per-layer thresholds for the 4-layer recovery (D-04).
    
        Each layer has its own max_attempts counter, independent escalation.
    
        Attributes:
            cosmetic_max_attempts: Max attempts for Layer 1 (cosmetic JSON repair).
            in_context_max_attempts: Max LLM re-call attempts for Layer 2 (FSM level).
            backoff_max_attempts: Max retry attempts for Layer 3 (backoff + jitter).
            escalate_timeout: Seconds to wait for human input on Layer 4.
        """
    
        cosmetic_max_attempts: int = 1
        in_context_max_attempts: int = 2
        backoff_max_attempts: int = 3
        escalate_timeout: float = 120.0
    ```

    注意：RecoveryLayer 使用 `from enum import IntEnum`，需要在文件顶部添加导入。

    **步骤 B — 在 executor.py 中添加 4 层恢复**

    注意：保留现有 `_execute_with_retry` 方法作为过渡（别删除），但要将其逻辑拆入 `_execute_with_recovery` 中。

    在 `ToolExecutor.__init__` 中添加可选参数：
    ```python
    def __init__(self, registry: ToolRegistry, recovery_config: RecoveryConfig | None = None) -> None:
        self._registry = registry
        self._recovery_cfg = recovery_config or RecoveryConfig()
    ```

    添加新的 `_execute_with_recovery` 方法：

    ```python
    async def _execute_with_recovery(
        self, metadata: ToolMetadata, validated_args: dict,
        session_id: str = "", tool_call_id: str = "",
    ) -> ToolResult:
        """Execute tool with 4-layer recovery escalation (D-03, D-04).
    
        Layer 1 — Cosmetic repair: fix JSON formatting errors in arguments.
        Layer 2 — In-context: handled at FSM level (structured error → LLM re-call).
        Layer 3 — Backoff: existing exponential backoff + jitter from RetryConfig.
        Layer 4 — Human escalation: publish event, return error result.
        """
        # ── Layer 1: Cosmetic repair ──────────────────────────────────
        for attempt in range(self._recovery_cfg.cosmetic_max_attempts):
            try:
                result = await self._execute_once(metadata, validated_args,
                                                   session_id, tool_call_id)
                return result  # Success → return immediately
            except (json.JSONDecodeError, TypeError) as exc:
                # Attempt cosmetic repair on first attempt
                if attempt == 0:
                    repaired = self._cosmetic_repair(validated_args, exc)
                    if repaired is not None:
                        validated_args = repaired
                        continue
                # Cannot repair → escalate
                # For JSONDecodeError, return error (Layer 1 exhaustion)
                return ToolResult.error(
                    error_msg=f"Layer 1 (cosmetic) exhausted: {exc}",
                    duration_ms=0.0,
                )
            except (TimeoutError, ConnectionError, asyncio.TimeoutError):
                # Network/timeout issues → not cosmetic, fall through to Layer 3
                break
            except Exception as exc:
                # Other exceptions → check category
                from loopai.tools.errors import classify_error
                from loopai.tools.types import ErrorCategory
                cat = classify_error(exc)
                if cat == ErrorCategory.TRANSIENT:
                    break  # Fall through to Layer 3
                # TOOL_EXECUTION or GUARD_VIOLATION → return error
                return ToolResult.error(
                    error_msg=f"{cat.value}: {exc}",
                    duration_ms=0.0,
                )

        # ── Layer 3: Full retry + backoff (existing logic) ────────────
        last_result: ToolResult | None = None
        retry_config = metadata.retry

        for attempt in range(retry_config.max_attempts):
            try:
                result = await self._execute_once(metadata, validated_args,
                                                   session_id, tool_call_id)
                return result  # Success
            except Exception as exc:
                from loopai.tools.errors import classify_error
                from loopai.tools.types import ErrorCategory
                category = classify_error(exc)

                if category == ErrorCategory.FATAL:
                    raise  # Fatal → terminate session

                if category == ErrorCategory.TRANSIENT:
                    msg = (f"Transient error "
                           f"(attempt {attempt + 1}/{retry_config.max_attempts}): {exc}")
                    last_result = ToolResult.error(error_msg=msg, duration_ms=0.0)
                    if attempt < retry_config.max_attempts - 1:
                        delay = retry_config.compute_delay(attempt)
                        await asyncio.sleep(delay)
                    continue

                # TOOL_EXECUTION or GUARD_VIOLATION → no retry
                return ToolResult.error(
                    error_msg=f"{category.value}: {exc}",
                    duration_ms=0.0,
                )

        # Layer 3 exhausted or last_result set from transient retries
        if last_result is not None:
            result_for_layer4 = last_result
        else:
            result_for_layer4 = ToolResult.error(
                error_msg="Layer 3 (backoff) exhausted with no result",
                duration_ms=0.0,
            )

        # ── Layer 4: Human escalation ─────────────────────────────────
        return result_for_layer4  # FSM 层会将此结果识别为需要人工升级
    ```

    添加 `_cosmetic_repair` 工具方法：

    ```python
    def _cosmetic_repair(self, validated_args: dict, exception: Exception) -> dict | None:
        """Attempt cosmetic repair of tool arguments based on the exception type.
    
        Current repairs:
        - TypeError: Check for common type mismatches and coerce
        - json.JSONDecodeError: (handled upstream in caller, not here)
    
        Args:
            validated_args: The current argument dict.
            exception: The exception that was raised.
    
        Returns:
            Repaired argument dict, or None if repair not possible.
        """
        exc_msg = str(exception).lower()
    
        # TypeError: try numeric string → number coercion
        if isinstance(exception, TypeError):
            repaired = dict(validated_args)
            for key, value in repaired.items():
                if isinstance(value, str):
                    # Try int conversion
                    try:
                        repaired[key] = int(value)
                        continue
                    except (ValueError, TypeError):
                        pass
                    # Try float conversion
                    try:
                        repaired[key] = float(value)
                        continue
                    except (ValueError, TypeError):
                        pass
            if repaired != validated_args:
                return repaired
    
        return None
    ```

    将 `execute` 方法中的 `_execute_with_retry` 调用替换为 `_execute_with_recovery`：

    ```python
    # Line 112 in original:
    return await self._execute_with_recovery(metadata, validated_args,
                                              session_id, tool_call_id)
    ```

    在文件顶部添加新的导入：
    ```python
    import json
    from enum import IntEnum
    from loopai.tools.types import RecoveryConfig, RecoveryLayer
    ```

    **步骤 C — 更新 ToolRegistry.get_schemas() 支持熔断过滤**

    ```python
    def get_schemas(self, exclude_open: set[str] | None = None) -> list[dict]:
        """Return OpenAI function-calling JSON Schemas for all registered tools.
    
        Args:
            exclude_open: Optional set of tool names to exclude (e.g., circuit-broken tools).
    
        Returns:
            List of OpenAI-compatible tool schema dicts, minus excluded tools.
        """
        tools = self._tools.values()
        if exclude_open:
            tools = [m for m in tools if m.name not in exclude_open]
        return [meta.to_openai_schema() for meta in tools]
    ```

    **步骤 D — 扩展 tests/test_tools.py**

    追加新测试类（在文件末尾）：

    `class TestRecoveryLayer`（5 个测试用例）：
    1. `test_cosmetic_repair_type_error` — TypeError 触发 Layer 1 修复（数值字符串 → 数字）
    2. `test_cosmetic_repair_exhausted` — 修复耗尽后返回错误
    3. `test_transient_falls_to_backoff` — TimeoutError 跳过 Layer 1 进入 Layer 3 退避
    4. `test_backoff_success` — 退避后工具成功（使用 RetryConfig 短延迟）
    5. `test_layer4_escalation_on_exhausted` — 所有层级耗尽后返回 `is_error=True` 的结果

    `class TestRegistryExcludeOpen`（2 个测试用例）：
    1. `test_get_schemas_filters_open_tools` — exclude_open 中指定的工具被过滤
    2. `test_get_schemas_no_exclude` — exclude_open=None 时返回全部工具

    注意：测试使用 `MagicMock` 模拟 tools 注册用于 Registry 测试。
  </action>
  <acceptance_criteria>
    1. ToolExecutor._execute_with_recovery 按层顺序执行：Layer 1→Layer 3→Layer 4
    2. Layer 1 在 TypeError 时修复参数（数字字符串→数字），重试后成功
    3. Layer 3 退避重试逻辑与原有 _execute_with_retry 行为一致
    4. ToolRegistry.get_schemas(exclude_open={"tool_a"}) 不包含 tool_a
    5. 全部测试通过：`python -m pytest tests/test_tools.py -x -q`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -q -k "TestRecoveryLayer or TestRegistryExcludeOpen" && python -m pytest tests/test_tools.py -x -q</automated>
  </verify>
  <done>
    ToolExecutor 4 层恢复管线完成，RecoveryConfig 类型定义完成，ToolRegistry 熔断过滤完成，全部测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: FSM 全组件集成 — GuardPipeline + CheckpointManager + CircuitBreaker + FailureRegistry</name>
  <files>
    src/loopai/state_machine/fsm.py
  </files>
  <read_first>
    src/loopai/state_machine/fsm.py — 完整阅读当前 FSM 实现（约 450 行），重点关注：
    - __init__ 参数和属性（第 56-80 行）
    - run() 主循环（第 83-119 行）
    - _handle_reason 中 GuardPipeline 插入点（第 146-150 行附近）
    - _handle_act 中 CircuitBreaker / FailureRegistry / RateLimitGuard 插入点（第 343-389 行附近）
  </read_first>
  <action>
    修改 `src/loopai/state_machine/fsm.py`。所有修改遵循现有的 TYPE_CHECKING 导入模式和 keyword-only 可选参数惯例。

    **步骤 A — 更新 TYPE_CHECKING 导入**

    在 `TYPE_CHECKING` 块中添加 Phase 4 组件的类型导入：
    ```python
    if TYPE_CHECKING:
        from loopai.context.compressor import ContextCompressor
        from loopai.events.bus import EventBus
        from loopai.llm.client import LLMClient
        from loopai.resilience.checkpoint import CheckpointManager
        from loopai.resilience.circuit_breaker import CircuitBreaker, CircuitState
        from loopai.resilience.failure_registry import FailureRegistry
        from loopai.session.context import Session
        from loopai.state_machine.guards import (
            BudgetGuard, GuardPipeline, LoopDetector,
            MessageValidator, PermissionGuard, RateLimitGuard, TokenGuard,
        )
        from loopai.tools.executor import ToolExecutor
        from loopai.tools.registry import ToolRegistry
    ```

    **步骤 B — 扩展 __init__ 构造函数**

    在现有参数基础上添加 5 个新的 keyword-only 可选参数（在 `*` 后面）：
    ```python
    def __init__(
        self,
        client: LLMClient,
        bus: EventBus,
        budget_guard: BudgetGuard,
        loop_detector: LoopDetector,
        message_validator: MessageValidator,
        registry: ToolRegistry,
        executor: ToolExecutor,
        permission_guard: PermissionGuard,
        *,
        # Phase 3
        token_guard: TokenGuard | None = None,
        compressor: ContextCompressor | None = None,
        # Phase 4 (new)
        guard_pipeline: GuardPipeline | None = None,
        checkpoint_manager: CheckpointManager | None = None,
        circuit_breaker: CircuitBreaker | None = None,
        failure_registry: FailureRegistry | None = None,
        rate_limit_guard: RateLimitGuard | None = None,
    ) -> None:
        # ... existing assignments ...
        self.guard_pipeline = guard_pipeline
        self.checkpoint_manager = checkpoint_manager
        self.circuit_breaker = circuit_breaker
        self.failure_registry = failure_registry
        self.rate_limit_guard = rate_limit_guard

        # Track recovery layers for Layer 2
        self._layer2_retry_count: dict[str, int] = {}  # tool_call_id → attempts
        self._layer2_max_attempts = 2
    ```

    **步骤 C — 修改 run() 主循环 — 添加检查点保存**

    在 `while` 循环的每个状态处理器之后添加检查点保存（在每次 state transition 后）：
    ```python
    while session.state not in (AgentState.FINISH, AgentState.ERROR):
        prev_state = session.state
        if session.state == AgentState.REASON:
            await self._handle_reason(session)
        elif session.state == AgentState.ACT:
            await self._handle_act(session)
        elif session.state == AgentState.OBSERVE:
            await self._handle_observe(session)
        else:
            session.state = AgentState.ERROR
            self._exit_reason = "unknown_state"

        # Checkpoint after every state transition (RES-01)
        if self.checkpoint_manager is not None:
            ckpt_data = self.checkpoint_manager.save(session)
            await self.bus.publish("checkpoint_saved", {
                "event_type": "checkpoint_saved",
                "session_id": session.session_id,
                "step_count": session.step_count,
                "state": session.state.value,
                "file_path": str(self.checkpoint_manager.filepath),
            })
    ```

    **步骤 D — 修改 _handle_reason — 添加 GuardPipeline**

    在 `_handle_reason` 中，在消息验证之后、TokenGuard 检查之前插入 GuardPipeline：

    ```python
    # Phase 4: GuardPipeline — sequential guard check (RES-04)
    if self.guard_pipeline is not None:
        pipeline_result = self.guard_pipeline.check(session.messages)
        if pipeline_result.action == "blocked":
            # Inject guard violation feedback into session
            session.add_message(
                "system",
                content=(
                    f"[{pipeline_result.guard_name}] {pipeline_result.detail}. "
                    "请调整策略后重试。"
                ),
            )
            # Publish guard violation event
            await self.bus.publish(
                "guard_violation",
                {
                    "event_type": "guard_violation",
                    "session_id": session.session_id,
                    "step_num": step_num,
                    "guard_name": pipeline_result.guard_name or "",
                    "detail": pipeline_result.detail or "",
                },
            )
            # Stay in REASON but the injected system message will constrain LLM
        elif pipeline_result.action == "compress" and self.compressor is not None:
            # Compression handled by existing TokenGuard logic below
            pass  # TokenGuard will trigger compression in Phase 3 code block
    ```

    GuardPipeline 插入位置：在 MessageValidator.validate() 之后、TokenGuard 和 BudgetGuard 之前。
    代码顺序变为：
    1. MessageValidator.validate()
    2. GuardPipeline.check() -- NEW
    3. TokenGuard.check() + compression (existing Phase 3)
    4. BudgetGuard.check()
    5. LLM call

    **步骤 E — 修改 _handle_act — 添加 CircuitBreaker + FailureRegistry + RateLimitGuard**

    在 _handle_act 的每个 tool 循环中（for tc in tool_calls_data:），在 Guard: loop detection 之后（第 309 行附近），按以下顺序插入：

    **E1 — FailureRegistry 检查**（在循环检测后、注册表查找前）：
    ```python
    # Phase 4: FailureRegistry check (RES-03)
    if self.failure_registry is not None:
        sig = self.loop_detector._signature(tool_name, raw_args if isinstance(raw_args, dict) else {})
        if self.failure_registry.should_skip(tool_name, sig):
            session.add_message(
                "tool",
                content=(
                    f"[SYSTEM] 操作 '{tool_name}(...)' 之前已失败并被注册。"
                    "请尝试不同的方法或参数。"
                ),
                tool_call_id=tool_call_id,
            )
            any_blocked = True
            continue
    ```

    **E2 — CircuitBreaker 检查**（在 FailureRegistry 之后、PermissionGuard 之前）：
    ```python
    # Phase 4: CircuitBreaker check (RES-06)
    if self.circuit_breaker is not None:
        allowed, cb_state = self.circuit_breaker.check(tool_name)
        if not allowed:
            session.add_message(
                "tool",
                content=(
                    f"[SYSTEM] 工具 '{tool_name}' 当前不可用 "
                    f"(熔断器状态: {cb_state.value})。请尝试其他方法。"
                ),
                tool_call_id=tool_call_id,
            )
            any_blocked = True
            continue
    ```

    **E3 — 工具执行后添加 CircuitBreaker.record + FailureRegistry.record**
    
    在现有 `result = await self.executor.execute(tool_name, raw_args)` 之后，在注入 tool_content 之前追加：

    ```python
    # Phase 4: CircuitBreaker record (RES-06)
    if self.circuit_breaker is not None:
        await self.circuit_breaker.record_with_session(
            tool_name, success=not result.is_error,
            session_id=session.session_id, bus=self.bus,
        )

    # Phase 4: FailureRegistry record on non-transient errors (RES-03)
    if self.failure_registry is not None and result.is_error:
        sig = self.loop_detector._signature(
            tool_name, raw_args if isinstance(raw_args, dict) else {}
        )
        self.failure_registry.record(
            tool_name, sig,
            result.error_message or "Unknown error",
        )

    # Phase 4: RateLimitGuard record (RES-04)
    if self.rate_limit_guard is not None and not result.is_error:
        self.rate_limit_guard.record_call(tool_name)
    ```

    **E4 — 使用过滤后仅传 OPEN 工具集给 Registry**

    修改 `_handle_reason` 中 `tool_schemas = self.registry.get_schemas() ...` 的调用：
    ```python
    # Phase 4: Circuit breaker filtering — exclude OPEN tools from LLM schema
    open_tools: set[str] | None = None
    if self.circuit_breaker is not None:
        open_tools = self.circuit_breaker.get_open_tools()

    tool_schemas = self.registry.get_schemas(exclude_open=open_tools) if self.registry else None
    ```

    **步骤 F — 不修改现有接口**

    注意：
    - `_handle_reason` 中现有的 `BudgetGuard.check()`、`TokenGuard.check()`、压缩逻辑完全不变
    - `_handle_act` 中现有的 `LoopDetector.check()`（已升级为三元组）解包时仅使用前两个返回值
    - `PermissionGuard.check()` 逻辑不变
    - 所有现有事件发布（StepStart、StepEnd、LoopDetected 等）不变

    **步骤 G — 运行全部现有 FSM 测试验证向后兼容**
  </action>
  <acceptance_criteria>
    1. GuardPipeline 在 _handle_reason 的消息验证后执行
    2. CheckpointManager 在每个状态转换后保存
    3. CircuitBreaker 在 _handle_act 中检查并记录每个工具调用
    4. FailureRegistry 在 _handle_act 中检查并记录失败
    5. RateLimitGuard.record_call() 在工具成功后调用
    6. get_schemas(exclude_open=open_tools) 过滤 OPEN 工具
    7. 所有现有测试通过：`python -m pytest tests/test_fsm.py -x -q`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_fsm.py -x -q</automated>
  </verify>
  <done>
    FSM 集成 Phase 4 全部组件完成：GuardPipeline、CheckpointManager、CircuitBreaker、FailureRegistry、RateLimitGuard 全部正确接线。
  </done>
</task>

<task type="auto">
  <name>Task 3: main.py run_session() 创建 Phase 4 组件 + 集成测试</name>
  <files>
    src/loopai/main.py
    tests/test_fsm.py
  </files>
  <read_first>
    src/loopai/main.py — 完整阅读 run_session()（第 39-141 行），理解现有的组件创建和注入模式
    src/loopai/config.py — 阅读 AgentConfig 现有字段（第 17-46 行）
  </read_first>
  <action>
    **步骤 A — 更新 main.py run_session()**

    添加 Phase 4 组件导入：
    ```python
    # Phase 4 resilience imports
    from loopai.resilience.checkpoint import CheckpointManager
    from loopai.resilience.circuit_breaker import CircuitBreaker
    from loopai.resilience.failure_registry import FailureRegistry
    from loopai.state_machine.guards import (
        BudgetGuard, CostGuard, GuardPipeline, LoopDetector,
        MessageValidator, PermissionGuard, RateLimitGuard, TokenGuard,
    )
    ```

    在 `# Phase 3 components` 块之后、FSM 创建之前，添加 Phase 4 组件的创建：

    ```python
    # ── Wire up resilience components (Phase 4) ──────────────────────────
    checkpoint_manager = CheckpointManager(session.session_id)
    circuit_breaker = CircuitBreaker()
    failure_registry = FailureRegistry(session.session_id)
    rate_limit_guard = RateLimitGuard()
    cost_guard = CostGuard()
    guard_pipeline = GuardPipeline([token_guard, cost_guard, rate_limit_guard])
    ```

    更新 FSM 构造调用，注入所有 Phase 4 组件：

    ```python
    fsm = ReActFSM(
        client, bus, budget_guard, loop_detector, message_validator,
        registry, executor, permission_guard,
        token_guard=token_guard,
        compressor=compressor,
        guard_pipeline=guard_pipeline,
        checkpoint_manager=checkpoint_manager,
        circuit_breaker=circuit_breaker,
        failure_registry=failure_registry,
        rate_limit_guard=rate_limit_guard,
    )
    ```

    注意：GuardPipeline 的参数顺序是 `[token_guard, cost_guard, rate_limit_guard]`。
    由于 RateLimitGuard 使用 `check(tool_name)` 而非 `check(messages)` 签名，GuardPipeline.check(messages)
    调用时 CostGuard 和 RateLimitGuard 的参数适配在 Plan 02 的 GuardPipeline 中已完成。
    但 RateLimitGuard 的 `check(tool_name)` 在 GuardPipeline 框架中不适用——因为 GuardPipeline.check(messages)
    对所有守卫传递 messages。这是一个已知的不匹配：RateLimitGuard 在 FSM 中被用于 `record_call()`，
    不参与 GuardPipeline 的顺序检查。

    修正 GuardPipeline 配置：RateLimitGuard 不在 pipeline 中，而是直接用于工具执行后的记录。

    ```python
    # GuardPipeline: 仅包含检查 messages 的守卫
    guard_pipeline = GuardPipeline([token_guard, cost_guard])
    # RateLimitGuard 不在 pipeline 中 — 它按工具名检查，在 _handle_act 中通过 record_call() 使用
    ```

    然后在 FSM 关闭后添加清理：

    ```python
    # ── Cleanup Phase 4 resources ──────────────────────────────────────
    if checkpoint_manager is not None:
        await checkpoint_manager.close()
    if failure_registry is not None:
        await failure_registry.close()
    ```

    **步骤 B — 移除不必要的 main.py 导入**

    注意：`main.py` 现有的 `from loopai.state_machine.guards import (...)` 不需要修改——这些在 Phase 3 已导入。

    **步骤 C — 添加集成测试**

    在 `tests/test_fsm.py` 文件末尾追加 `class TestResilienceIntegration`：

    注意：这些测试使用 mock + spy 验证 FSM 正确调用了 Phase 4 组件。

    `class TestResilienceIntegration`（5 个测试用例）：

    1. `test_checkpoint_saved_event` — FSM 运行后在 EventBus 中可找到 checkpoint_saved 事件
    2. `test_circuit_breaker_check_called` — 工具执行时 FSM 调用了 CircuitBreaker.check()
    3. `test_circuit_breaker_record_called` — 工具执行后 FSM 调用了 CircuitBreaker.record_with_session()
    4. `test_guard_pipeline_ok_passthrough` — GuardPipeline 返回 ok 时 LLM 正常调用
    5. `test_failure_registry_records_on_error` — 工具失败时 FailureRegistry.record() 被调用

    集成测试辅助函数（在测试类中）：
    ```python
    def _make_resilience_fsm(self, client, bus, budget_guard, loop_detector,
                              message_validator, **kwargs):
        """Helper: create ReActFSM with Phase 4 component mocks."""
        from loopai.state_machine.fsm import ReActFSM
        from unittest.mock import MagicMock, AsyncMock
        from loopai.tools.types import ToolMetadata, ToolResult, PermissionLevel

        registry = MagicMock()
        executor = MagicMock()
        executor.execute = AsyncMock(
            return_value=ToolResult.success(data="output", duration_ms=10.0)
        )
        permission_guard = MagicMock()
        permission_guard.check = AsyncMock(return_value=(True, "allow"))

        meta = ToolMetadata(
            name="bash", description="Execute bash",
            permission_level=PermissionLevel.SAFE, timeout=60.0,
        )
        registry.get.return_value = meta
        registry.get_schemas.return_value = [meta.to_openai_schema()]

        checkpoint_manager = MagicMock()
        checkpoint_manager.save = MagicMock(return_value={"state": "finished"})

        circuit_breaker = MagicMock()
        circuit_breaker.check = MagicMock(return_value=(True, "closed"))
        circuit_breaker.record_with_session = AsyncMock()
        circuit_breaker.get_open_tools = MagicMock(return_value=set())

        failure_registry = MagicMock()
        failure_registry.should_skip = MagicMock(return_value=False)
        failure_registry.record = MagicMock()

        guard_pipeline = MagicMock()
        guard_pipeline.check = MagicMock(
            return_value=GuardResult(action="ok")
        )

        rate_limit_guard = MagicMock()
        rate_limit_guard.record_call = MagicMock()

        fsm = ReActFSM(
            client, bus, budget_guard, loop_detector, message_validator,
            registry, executor, permission_guard,
            **{**kwargs,
               "checkpoint_manager": checkpoint_manager,
               "circuit_breaker": circuit_breaker,
               "failure_registry": failure_registry,
               "guard_pipeline": guard_pipeline,
               "rate_limit_guard": rate_limit_guard,
            },
        )
        return fsm, {
            "checkpoint_manager": checkpoint_manager,
            "circuit_breaker": circuit_breaker,
            "failure_registry": failure_registry,
            "guard_pipeline": guard_pipeline,
            "rate_limit_guard": rate_limit_guard,
        }
    ```

    **测试 1: test_checkpoint_saved_event：**
    - 使用 `_make_resilience_fsm` 创建 FSM
    - FSM 运行后，验证 `bus.replay("checkpoint_saved")` 结果 > 0
    - 验证 checkpoint_manager.save 被调用

    **测试 2: test_circuit_breaker_check_called：**
    - 使用带 tool_calls 的 LLM 响应
    - FSM 运行后，验证 `circuit_breaker.check` 被调用且 tool_name 为 "bash"

    **测试 3: test_circuit_breaker_record_called：**
    - 工具执行后，验证 `circuit_breaker.record_with_session` 被调用

    **测试 4: test_guard_pipeline_ok_passthrough：**
    - GuardPipeline.check 返回 action="ok"
    - 验证 LLM 被正常调用（client.complete 被调用）

    **测试 5: test_failure_registry_records_on_error：**
    - executor.execute 返回 ToolResult.error
    - 验证 failure_registry.record 被调用
  </action>
  <acceptance_criteria>
    1. `main.py` 正确创建并注入 Phase 4 组件
    2. 集成测试验证 FSM 正确调用所有 Phase 4 组件
    3. `python -m pytest tests/test_fsm.py -x -q` — 全部 FSM 测试（原有 + 新增）通过
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_fsm.py -x -q</automated>
  </verify>
  <done>
    main.py 更新完成，Phase 4 组件创建和注入完成，集成测试验证 Phase 4 组件在 FSM 中正确工作。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSM → GuardPipeline | 守卫检查结果可能注入系统消息到 session |
| FSM → CheckpointManager | Session 状态序列化到磁盘 |
| FSM → CircuitBreaker | 工具执行结果影响熔断器状态 |
| FSM → failure_registry | 失败工具记录持久化到磁盘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | GuardPipeline system msg block | mitigate | GuardPipeline 注入的 system 消息不包含消息内容，只有守卫名称和通用提示。无敏感信息泄露。 |
| T-04-08 | Denial of Service | Layer 2 in-context retry | mitigate | FSM 的 Layer 2 重试由 `_layer2_max_attempts=2` 硬限制，防止无限 LLM 递归调用。每个 tool_call_id 独立计数。 |
| T-04-09 | Elevation of Privilege | CircuitBreaker.get_open_tools() | accept | get_open_tools() 仅从 LLM schema 中过滤工具。不会阻止用户通过其他方式调用。熔断器是辅助性防护，不承担安全边界责任。 |
</threat_model>

<verification>
```bash
# ToolExecutor 4-layer recovery + Registry filtering tests
python -m pytest tests/test_tools.py -x -q

# FSM integration tests
python -m pytest tests/test_fsm.py -x -q

# Full Phase 3 + 4 test suite
python -m pytest tests/test_tools.py tests/test_fsm.py tests/test_guards.py \
  tests/test_checkpoint.py tests/test_failure_registry.py tests/test_circuit_breaker.py \
  -x -q
```
</verification>

<success_criteria>
- ToolExecutor 支持 4 层恢复（外观修复→退避→人工升级）
- ToolRegistry.get_schemas() 支持 exclude_open 过滤
- FSM 集成所有 Phase 4 组件，接线点在正确的状态处理器中
- main.py run_session() 创建并注入所有组件
- 集成测试验证全组件交互
- 所有测试通过（原有 + 新增）
</success_criteria>

<output>
After completion, create `.planning/phases/04-resilience/04-03-SUMMARY.md`
</output>
