---
phase: 04-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loopai/state_machine/guards.py
  - tests/test_guards.py
autonomous: true
requirements: [RES-02, RES-04]
user_setup: []

must_haves:
  truths:
    - "LoopDetector 在检测到循环时返回分类类型（完全相同 / 相同工具不同参数 / 卡住）"
    - "循环检测触发时，LLM 收到元认知提示而不是通用阻止消息"
    - "守卫管道按顺序执行 TokenGuard → CostGuard → RateLimitGuard"
    - "CostGuard 根据 token 计数估算成本并返回信号"
    - "RateLimitGuard 限制同一工具的调用频率"
    - "守卫管道在第一个阻塞守卫处短路并返回其名称和详情"
  artifacts:
    - path: "src/loopai/state_machine/guards.py"
      provides: "GuardPipeline, CostGuard, RateLimitGuard, upgraded LoopDetector"
      min_lines: 400
      contains: "class GuardPipeline"
  key_links:
    - from: "guards.py"
      to: "fsm.py"
      via: "GuardPipeline used by FSM._handle_reason()"
      pattern: "guard_pipeline"
    - from: "LoopDetector.check()"
      to: "FailureRegistry._signature()"
      via: "Same _signature() hash format for consistency"
      pattern: "_signature"
---

<objective>
实现守卫管道（GuardPipeline + CostGuard + RateLimitGuard）和循环检测升级（分类 + 元认知提示）。

Purpose: GuardPipeline 编排 TokenGuard（来自 Phase 3 已有）、CostGuard（新增）和 RateLimitGuard（新增），顺序检查 LLM 调用前的预算/成本/速率条件（RES-04）。升级 LoopDetector 从二值 allow/block 到返回循环分类（LOOP_EXACT_SAME / LOOP_SAME_TOOL / LOOP_STUCK），在 block 时注入元认知提示让 LLM 理解为什么被阻止（RES-02）。

Output:
- GuardPipeline 类（短路由机制）
- CostGuard 类（基于 token 计数的成本估算）
- RateLimitGuard 类（基于滑动窗口的速率限制）
- LoopDetector 升级（分类类型 + 元认知提示注入）
- 扩展测试覆盖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-resilience/04-CONTEXT.md
@.planning/phases/04-resilience/04-RESEARCH.md

<interfaces>
<!-- Existing guard patterns and types that this plan extends -->

From src/loopai/state_machine/guards.py — existing patterns:
```python
class LoopDetector:
    def __init__(self, window_size: int = 20, warn_threshold: int = 3, block_threshold: int = 5):
        self._window: deque[tuple[str, str]] = deque(maxlen=window_size)
        ...

    def check(self, tool_name: str, arguments: dict) -> tuple[bool, str]:
        """Returns (should_proceed, action). action is one of "allow","warn","block","force_exit"."""
        sig = self._signature(tool_name, arguments)
        ...
        return (True/False, "allow"/"warn"/"block"/"force_exit")

    def reset(self) -> None:
        ...

    @staticmethod
    def _signature(tool_name: str, arguments: dict) -> str:
        args_json = json.dumps(arguments, sort_keys=True, ensure_ascii=True)
        raw = f"{tool_name}:{args_json}"
        return hashlib.sha256(raw.encode()).hexdigest()[:16]

class TokenGuard:
    def __init__(self, token_counter, window_size: int = 128000, threshold: float = 0.75):
        ...

    def check(self, messages: list[dict]) -> tuple[str, int, int]:
        """Returns ("ok"/"compress", token_count, threshold_tokens)."""

class BudgetGuard:
    def check(self, step_count: int, messages: list[dict]) -> tuple[bool, list[dict], str | None]:
        """Returns (True, modified_messages, action) where action is None/"warn"/"final"."""
    def check_unreachable(self, is_failure: bool) -> str | None:
        """Returns None or "unreachable"."""
```

From src/loopai/session/context.py — TokenCounter TYPE_CHECKING import:
```python
if TYPE_CHECKING:
    from loopai.context.token_counter import TokenCounter
```

New types that will be added to guards.py:
```python
from enum import StrEnum

class LoopClassification(StrEnum):
    LOOP_EXACT_SAME = "exact_same"   # Same tool + same arguments
    LOOP_SAME_TOOL = "same_tool"     # Same tool, different arguments (stuck on tool choice)
    LOOP_STUCK = "stuck"             # Different tools but no progress

@dataclass
class GuardResult:
    action: str          # "ok", "warn", "compress", "blocked"
    guard_name: str | None = None
    detail: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GuardPipeline + CostGuard + RateLimitGuard</name>
  <files>
    src/loopai/state_machine/guards.py
    tests/test_guards.py
  </files>
  <read_first>
    src/loopai/state_machine/guards.py — 阅读 TokenGuard.check() 签名（第 478-497 行）和现有导入模式
    src/loopai/tools/types.py — 阅读 ErrorCategory 和 PermissionLevel 的 StrEnum 模式
    src/loopai/context/token_counter.py — 阅读 TokenCounter 接口（如需导入）
  </read_first>
  <action>
    在 `guards.py` 文件末尾追加（在 PermissionGuard 和 TokenGuard 定义之后），所有新增代码使用 4 空格缩进与现有风格一致。

    **步骤 A — 添加导入**

    在文件顶部现有导入区域追加：
    ```python
    import math
    from dataclasses import dataclass
    from enum import StrEnum
    ```

    **步骤 B — GuardResult 数据类**

    ```python
    @dataclass
    class GuardResult:
        """Result of a guard check in the pipeline.
    
        Attributes:
            action: "ok" (all clear), "compress" (compression needed from TokenGuard),
                    "blocked" (guard violation), "warn" (approaching limit).
            guard_name: Name of the guard class that produced this result.
            detail: Human-readable detail message for the blocking guard.
        """
        action: str
        guard_name: str | None = None
        detail: str | None = None
    ```

    **步骤 C — CostGuard 类**

    ```python
    class CostGuard:
        """Cost estimation guard — estimates LLM call cost from token count.

        Uses a hardcoded model pricing table. Returns a signal when estimated
        cost exceeds the per-call budget.

        Attributes:
            model_cost_per_1k: Dict mapping model prefix to (input_cost_per_1k, output_cost_per_1k).
            max_cost_per_call: Maximum allowed cost per LLM call in USD (default 0.05).
        """

        _DEFAULT_PRICING: dict[str, tuple[float, float]] = {
            "gpt-4o": (0.0025, 0.01),
            "gpt-4o-mini": (0.00015, 0.0006),
            "gpt-4": (0.03, 0.06),
            "gpt-4-turbo": (0.01, 0.03),
            "gpt-3.5-turbo": (0.0005, 0.0015),
        }

        def __init__(
            self,
            max_cost_per_call: float = 0.05,
            pricing: dict[str, tuple[float, float]] | None = None,
        ) -> None:
            self.max_cost_per_call = max_cost_per_call
            self._pricing = pricing or dict(self._DEFAULT_PRICING)

        def estimate_cost(self, token_count: int, model_name: str = "gpt-4o") -> float:
            """Estimate cost for a single LLM call.

            Uses model prefix matching (e.g., "gpt-4o-2024-08-06" matches "gpt-4o").
            If model not found in pricing table, uses gpt-4o pricing as fallback.
            
            Returns estimated cost in USD (rounded to 6 decimal places).
            """
            input_cost, output_cost = self._get_rates(model_name)
            # Conservative estimate: assume output is ~30% of total tokens
            input_tokens = int(token_count * 0.7)
            output_tokens = token_count - input_tokens
            cost = (input_tokens / 1000) * input_cost + (output_tokens / 1000) * output_cost
            return round(cost, 6)

        def _get_rates(self, model_name: str) -> tuple[float, float]:
            """Look up pricing rates for a model name with prefix matching."""
            for prefix, rates in self._pricing.items():
                if model_name.startswith(prefix):
                    return rates
            return self._pricing.get("gpt-4o", (0.0025, 0.01))

        def check(self, messages: list[dict], token_count: int | None = None,
                  model_name: str = "gpt-4o") -> GuardResult:
            """Check if estimated cost is within budget.

            Args:
                messages: Message list (used if token_count not provided — unused in this version).
                token_count: Pre-computed token count. If None, uses len(messages) * 500 as rough estimate.
                model_name: Model name for pricing lookup.

            Returns:
                GuardResult with action="ok" if cost within budget, or "blocked" if over budget.
            """
            if token_count is None:
                token_count = max(len(messages) * 500, 1000)  # Rough estimate fallback
            
            cost = self.estimate_cost(token_count, model_name)
            if cost > self.max_cost_per_call:
                return GuardResult(
                    action="blocked",
                    guard_name="CostGuard",
                    detail=f"Estimated cost ${cost:.6f} exceeds limit ${self.max_cost_per_call:.4f}",
                )
            return GuardResult(action="ok", guard_name="CostGuard")
    ```

    **步骤 D — RateLimitGuard 类**

    ```python
    class RateLimitGuard:
        """Rate limit guard — limits tool call frequency within a time window.

        Uses a sliding time window (not sliding count) per tool.
        Tracks call timestamps and blocks if calls exceed the limit within the window.

        Attributes:
            max_calls: Maximum allowed calls within the time window (default 10).
            window_seconds: Time window in seconds (default 60.0).
        """

        def __init__(self, max_calls: int = 10, window_seconds: float = 60.0) -> None:
            self.max_calls = max_calls
            self.window_seconds = window_seconds
            self._call_times: dict[str, list[float]] = {}  # tool_name -> [timestamps]

        def check(self, tool_name: str) -> GuardResult:
            """Check if tool has exceeded its rate limit.

            Args:
                tool_name: The tool to check rate limit for.

            Returns:
                GuardResult with action="ok" if within limit, "blocked" if exceeded.
            """
            now = time.monotonic()
            timestamps = self._call_times.get(tool_name, [])

            # Prune timestamps outside the window
            cutoff = now - self.window_seconds
            active = [t for t in timestamps if t >= cutoff]
            self._call_times[tool_name] = active

            if len(active) >= self.max_calls:
                oldest = active[0] if active else now
                retry_after = round(self.window_seconds - (now - oldest), 1)
                return GuardResult(
                    action="blocked",
                    guard_name="RateLimitGuard",
                    detail=f"Tool '{tool_name}' rate limit exceeded: {len(active)} calls in {self.window_seconds}s window. Retry after {retry_after}s.",
                )
            return GuardResult(action="ok", guard_name="RateLimitGuard")

        def record_call(self, tool_name: str) -> None:
            """Record a tool call for rate limiting.

            Must be called after the tool executes to update the rate counter.
            """
            if tool_name not in self._call_times:
                self._call_times[tool_name] = []
            self._call_times[tool_name].append(time.monotonic())
    ```

    **步骤 E — GuardPipeline 类**

    ```python
    class GuardPipeline:
        """Sequential guard pipeline with short-circuit on first non-ok result.

        Runs each guard's check() in order. If any guard returns action != "ok",
        the pipeline short-circuits and returns that guard's result immediately.

        Guards are injected as callables matching the Protocol:
            def check(messages) -> GuardResult
        """

        def __init__(self, guards: list[Any]) -> None:
            self._guards = guards

        def check(self, messages: list[dict]) -> GuardResult:
            """Run each guard sequentially. Short-circuit on first non-ok result.

            Args:
                messages: The current message list (passed to each guard).

            Returns:
                GuardResult from the first blocking guard, or GuardResult(action="ok")
                if all guards pass.
            """
            for guard in self._guards:
                result = guard.check(messages)
                if result.action != "ok":
                    return result
            return GuardResult(action="ok")
    ```

    **步骤 F — 更新 __init__.py 导出（可选，but not needed since guards.py is imported directly）**

    注意：不要改变现有代码的任何行为。TokenGuard、BudgetGuard、LoopDetector、MessageValidator、PermissionGuard 的现有签名必须完全不变。

    **步骤 G — guard_pipeline_only_check 方法在 TokenGuard 适配**

    不需要修改 TokenGuard 的现有 check() 方法。GuardPipeline.check(messages) 调用 TokenGuard.check(messages)，
    TokenGuard 返回 (str, int, int) 三元组而不是 GuardResult。在 GuardPipeline 内过滤一下吧。

    等一下 — 需要确保 GuardPipeline 能处理 TokenGuard 的返回格式。TokenGuard.check() 返回 `("ok"/"compress", token_count, threshold_tokens)`，不是 GuardResult。

    修改 GuardPipeline 适配：
    ```python
    def check(self, messages: list[dict]) -> GuardResult:
        for guard in self._guards:
            raw = guard.check(messages)
            # Normalize: handle both GuardResult objects and tuple returns
            if isinstance(raw, GuardResult):
                result = raw
            elif isinstance(raw, tuple):
                action = raw[0]
                name = guard.__class__.__name__
                if action == "ok":
                    result = GuardResult(action="ok", guard_name=name)
                elif action == "compress":
                    result = GuardResult(action="compress", guard_name=name,
                        detail=f"Token count {raw[1]} >= threshold {raw[2]}")
                else:
                    result = GuardResult(action="blocked", guard_name=name,
                        detail=f"Guard returned action={action}")
            else:
                result = GuardResult(action="ok", guard_name=guard.__class__.__name__)
            
            if result.action != "ok":
                return result
        return GuardResult(action="ok")
    ```

    **步骤 H — 扩展 tests/test_guards.py**

    追加新测试类（使用现有测试文件末尾追加的模式）：

    `class TestGuardPipeline`（3 个测试用例）：
    1. `test_all_ok` — 所有守卫返回 ok 时 Pipeline 返回 action="ok"
    2. `test_short_circuit_first_blocked` — 第二个守卫返回 blocked 时 Pipeline 不执行第三个守卫（mock 验证调用次数）
    3. `test_compress_signal_passthrough` — TokenGuard 返回 "compress" 时 Pipeline 透传

    `class TestCostGuard`（3 个测试用例）：
    1. `test_estimate_cost_within_budget` — gpt-4o 1000 tokens 成本低于 0.05
    2. `test_estimate_cost_exceeds_budget` — 大量 token 触发 blocked
    3. `test_model_prefix_matching` — "gpt-4o-2024-08-06" 匹配到 "gpt-4o" 定价
    4. `test_unknown_model_falls_back` — 未知模型回退到 gpt-4o 定价

    `class TestRateLimitGuard`（3 个测试用例）：
    1. `test_within_limit` — 调用次数在限制内返回 ok
    2. `test_exceeds_limit` — 超过限制后返回 blocked
    3. `test_window_prunes_old_entries` — 旧时间戳被修剪，不再计数
  </action>
  <acceptance_criteria>
    1. GuardPipeline 顺序执行守卫，在第一个非 ok 处短路
    2. TokenGuard 的 tuple 返回格式被正确适配为 GuardResult
    3. CostGuard 正确估算成本，在超过预算时返回 blocked
    4. RateLimitGuard 正确计数并阻止超速调用
    5. 测试通过：`python -m pytest tests/test_guards.py -x -q -k "TestGuardPipeline or TestCostGuard or TestRateLimitGuard"`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_guards.py -x -q -k "TestGuardPipeline or TestCostGuard or TestRateLimitGuard"</automated>
  </verify>
  <done>
    GuardPipeline、CostGuard、RateLimitGuard 实现完成，适配 TokenGuard 的不同返回格式，Pipeline 短路由机制验证通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: LoopDetector 升级 — 分类 + 元认知提示</name>
  <files>
    src/loopai/state_machine/guards.py
    tests/test_guards.py
  </files>
  <read_first>
    src/loopai/state_machine/guards.py — 阅读 LoopDetector 类的详细实现（第 35-120 行），特别关注 check() 方法返回逻辑和 _consecutive_count 追踪
  </read_first>
  <action>
    升级现有的 `LoopDetector` 类，不要改变已有的 `__init__` 签名和 `reset()` 方法。

    **步骤 A — 添加 LoopClassification 枚举（在 guards.py 文件顶部，其他枚举定义附近）**

    ```python
    class LoopClassification(StrEnum):
        """Classification of detected loop patterns for metacognitive prompting (RES-02).
    
        LOOP_EXACT_SAME: Same tool called with the same arguments repeatedly.
        LOOP_SAME_TOOL: Same tool called repeatedly but with different arguments.
        LOOP_STUCK: Different tools called but no meaningful progress (heuristic).
        """
        LOOP_EXACT_SAME = "exact_same"
        LOOP_SAME_TOOL = "same_tool"
        LOOP_STUCK = "stuck"
    ```

    **步骤 B — 修改 LoopDetector.check() 方法**

    将现有返回类型从 `tuple[bool, str]` 扩展为 `tuple[bool, str, LoopClassification | None]`。
    第三个返回值是 `LoopClassification` 枚举或 `None`（当没有检测到循环时）。

    ```python
    def check(self, tool_name: str, arguments: dict) -> tuple[bool, str, LoopClassification | None]:
        """检查此工具调用是否构成循环。

        Args:
            tool_name: 被调用的工具名称。
            arguments: 工具参数字典。

        Returns:
            (should_proceed, action, classification) 三元组。
            classification 为 None 表示无循环，否则为循环分类类型。
        """
        sig = self._signature(tool_name, arguments)

        if sig == self._last_signature:
            self._consecutive_count += 1
        else:
            self._consecutive_count = 1
            self._last_signature = sig

        self._window.append((tool_name, sig))

        classification: LoopClassification | None = None

        if self._consecutive_count >= self._warn_threshold:
            # Determine classification based on window analysis
            # LOOP_EXACT_SAME: all recent entries have same signature
            # LOOP_SAME_TOOL: same tool but different signatures
            # LOOP_STUCK: different tools, frequent switching
            recent = list(self._window)[-min(5, len(self._window)):]
            if not recent:
                pass
            elif all(t == tool_name and s == sig for t, s in recent):
                classification = LoopClassification.LOOP_EXACT_SAME
            elif all(t == tool_name for t, _ in recent):
                classification = LoopClassification.LOOP_SAME_TOOL
            else:
                classification = LoopClassification.LOOP_STUCK

        if self._consecutive_count > self._block_threshold:
            return (False, "force_exit", classification)
        elif self._consecutive_count >= self._block_threshold:
            return (False, "block", classification)
        elif self._consecutive_count >= self._warn_threshold:
            return (True, "warn", classification)
        return (True, "allow", classification)
    ```

    **步骤 C — 添加元认知提示生成方法**

    ```python
    @staticmethod
    def get_meta_prompt(tool_name: str, classification: LoopClassification | None,
                        consecutive_count: int) -> str:
        """Generate a metacognitive prompt explaining why the loop was detected.

        Args:
            tool_name: The tool being called.
            classification: The loop classification type.
            consecutive_count: Number of consecutive identical calls.

        Returns:
            A Chinese metacognitive prompt string to inject as system message.
        """
        if classification == LoopClassification.LOOP_EXACT_SAME:
            return (
                f"[元认知提示] 你已经连续 {consecutive_count} 次使用完全相同参数调用 "
                f"'{tool_name}'，每次都得到相同结果。继续重复不会改变结果。"
                f"请尝试以下策略之一：\n"
                f"1. 换个思路，使用不同的工具或参数\n"
                f"2. 检查已有信息是否能直接回答问题\n"
                f"3. 如果无法完成，直接告知用户并解释原因"
            )
        elif classification == LoopClassification.LOOP_SAME_TOOL:
            return (
                f"[元认知提示] 你反复使用 '{tool_name}' 工具（{consecutive_count} 次），"
                f"虽然参数不同但未取得实质进展。请考虑：\n"
                f"1. 是否需要换一个工具？\n"
                f"2. 已有信息是否足够回答用户？\n"
                f"3. 尝试不同的方法解决问题"
            )
        elif classification == LoopClassification.LOOP_STUCK:
            return (
                f"[元认知提示] 检测到你可能在执行中卡住了 — "
                f"最近 {consecutive_count} 步你尝试了不同的工具但没有取得进展。"
                f"请暂停并重新评估：\n"
                f"1. 当前目标是什么？\n"
                f"2. 哪些操作真正有助于达成目标？\n"
                f"3. 是否需要向用户请求更多信息？"
            )
        return (
            f"[元认知提示] 检测到重复调用模式（{consecutive_count} 次）。"
            f"请尝试不同的方法或直接给出你的结论。"
        )
    ```

    **步骤 D — 更新 FSM 中的调用点（fsm.py 的 _handle_act）**

    注意：fsm.py 的修改在 Plan 03 中完成。当前 Plan 02 仅升级 LoopDetector 的接口。
    fs.py 中现有的 `self.loop_detector.check(tool_name, raw_args)` 调用只解包前两个返回值，
    所以添加第三个返回值是向后兼容的——在 Plan 03 之前不会利用分类信息。

    **步骤 E — 更新测试文件**

    注意：现有测试中 `LoopDetector.check()` 返回的是 `tuple[bool, str]`，现在变为 `tuple[bool, str, LoopClassification | None]`。
    需要更新测试中对 `check()` 返回值的断言。
    在 `test_guards.py` 文件末尾追加新测试类：

    `class TestLoopDetectorUpgrade`（5 个测试用例）：
    1. `test_first_call_classification_none` — 第一次调用返回 classification=None
    2. `test_exact_same_loop_classified` — 连续 3 次相同工具+参数返回 LOOP_EXACT_SAME
    3. `test_same_tool_diff_args_classified` — 同一工具不同参数超过阈值返回 LOOP_SAME_TOOL
    4. `test_stuck_classification` — 不同工具但频繁切换返回 LOOP_STUCK
    5. `test_meta_prompt_exact_same` — get_meta_prompt 对 EXACT_SAME 返回包含工具名的中文提示
    6. `test_meta_prompt_same_tool` — get_meta_prompt 对 SAME_TOOL 返回提示
    7. `test_meta_prompt_stuck` — get_meta_prompt 对 STUCK 返回提示

    同时更新现有 LoopDetector 测试（TestLoopDetector 类中的方法）以解包三元组。
    - `should_proceed, action = detector.check(...)` → `should_proceed, action, _ = detector.check(...)`
    - 验证现有行为的断言保持不变。
  </action>
  <acceptance_criteria>
    1. LoopDetector.check() 返回三元组 (bool, str, LoopClassification | None)
    2. 循环分类正确：完全相同 / 同工具不同参数 / 卡住
    3. get_meta_prompt() 根据分类生成不同的中文元认知提示
    4. 全部 guards 测试通过：`python -m pytest tests/test_guards.py -x -q`
    5. 现有 FSM 测试不受影响：`python -m pytest tests/test_fsm.py -x -q`（因为 fsm.py 解包时忽略第三个值）
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_guards.py -x -q</automated>
  </verify>
  <done>
    LoopDetector 升级完成，check() 返回三元组包含 LoopClassification，元认知提示根据分类类型生成。全部测试通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GuardPipeline → LLM call | 守卫检查结果影响 LLM 调用是否继续 |
| LoopDetector → session messages | 循环检测后注入元认知提示到消息列表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | CostGuard | accept | CostGuard 仅基于 token 计数做算术运算，不接触敏感数据。发布的事件只包含成本估计值，不包含消息内容。 |
| T-04-05 | Denial of Service | RateLimitGuard | mitigate | RateLimitGuard 的限制条件是可配置的（max_calls, window_seconds），被限制时 LLM 收到明确的"速率限制"反馈，不会导致死锁。Executor 层面不因速率限制而停止执行非受限工具。 |
| T-04-06 | Tampering | LoopClassification metadata | mitigate | 元认知提示使用固定的中文模板字符串，不会被 LLM 输出污染。模板在代码中硬编码，非 LLM 生成。 |
</threat_model>

<verification>
```bash
# GuardPipeline + new guards tests
python -m pytest tests/test_guards.py -x -q -k "TestGuardPipeline or TestCostGuard or TestRateLimitGuard"

# LoopDetector upgrade tests
python -m pytest tests/test_guards.py -x -q -k "TestLoopDetectorUpgrade or TestLoopDetector"

# Full guards suite
python -m pytest tests/test_guards.py -x -q

# Existing FSM tests not broken (third return value ignored)
python -m pytest tests/test_fsm.py -x -q
```
</verification>

<success_criteria>
- GuardPipeline 按 TokenGuard → CostGuard → RateLimitGuard 顺序检查，短路由机制正确
- CostGuard 使用模型定价表估算成本
- RateLimitGuard 使用滑动窗口限制工具调用频率
- LoopDetector 升级后返回循环分类信息 + 元认知提示
- 全部测试通过（guards + fsm）
</success_criteria>

<output>
After completion, create `.planning/phases/04-resilience/04-02-SUMMARY.md`
</output>
